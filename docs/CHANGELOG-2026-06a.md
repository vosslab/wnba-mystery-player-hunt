## 2026-06-11

### Additions and New Features

- Added `file_utils.sync_report(name: str, lines: list[str]) -> str`: the single report-writing
  site for hygiene tests. Non-empty `lines` truncate-writes the full report (one `\n` per line,
  one trailing `\n`); empty `lines` purges the stale report file via `purge_report`; returns the
  absolute path. Pinned by `tests/meta/test_sync_report.py`. Documented in
  [docs/PYTEST_STYLE.md](PYTEST_STYLE.md) "Hygiene report files" and the
  [tests/TESTS_README.md](../tests/TESTS_README.md) helper list.
- Added canonical hygiene-report pattern: a module-scope autouse `collect_report` fixture owns
  the single `sync_report` call (precomputes `VIOLATIONS_BY_FILE: dict[str, list[str]]` over all
  `FILES`, syntax/parse errors captured as `SyntaxError` entries, downstream checks skipped for
  that file); each parametrized per-file test does plain `assert rel not in VIOLATIONS_BY_FILE`.
  Writer-style tests build a `lines` list and call `sync_report` always. Fail via plain `assert`;
  precondition guards use `RuntimeError`. Report is a full snapshot written when dirty, purged
  when clean. Any module run synchronizes the full report, even with `-k` filtering.

- Added `repolib/process.py` holding the shared per-repo orchestration (`process_repo`, `apply_file_bucket`, `remove_deprecated_tests`, `exit_code_for`) and a single context contract `build_context_for_repo(repo_path, dry_run, bootstrap, auto_discover, write_marker)` used by both entry scripts.
- Extended repo-root report coverage to `tests/test_function_typing.py`, `tests/test_indentation.py`, `tests/test_test_naming_conventions.py`, and `tests/test_pytest_hygiene.py` via the shared report helpers (`report_path`, `purge_report`, `append_report`, `rel_to_root`) in `tests/file_utils.py`; each test now purges its report at module start and appends violation blocks with a report path reference in the AssertionError on failure; `tests/test_pytest_hygiene.py` writes `report_pytest_hygiene.txt` at repo root on failure.
- Added three centralized helpers to `tests/file_utils.py`: `iter_imports(tree)` (yields all `ast.Import` and `ast.ImportFrom` nodes from a parsed module tree), `rel_to_root(path, repo_root=None)` (returns a repo-relative POSIX string for use in test ids and messages), and `run_fixer_script(name, target)` (shared subprocess wrapper for the ASCII and whitespace fixer scripts, replacing duplicated inline subprocess calls in two hygiene tests).
- Added two new repo-wide vendored hygiene tests: `tests/test_function_typing.py` (AST-based guard that bans `import typing` / `from typing import` and requires both param and return type annotations on every `def`; uses builtin generics and PEP 604 unions only) and `tests/test_pytest_hygiene.py` (AST guard ensuring hygiene tests do not reintroduce a local `SKIP_DIRS`, `path_has_skip_dir`, or `gather_*` discovery scaffold -- the centralized forms in `tests/file_utils.py` are the only allowed home).
- Added `meta/tools/rename_git_file_utils.sh`: one-off maintenance script that rewrites consumer-authored references to the old shared helper `git_file_utils` to its current name `file_utils` across `tests/*.py` and `docs/*.md` in the current directory. Skips CHANGELOG files (whose history legitimately records the old name), is idempotent, and uses GNU `sed -i`. Run from a consumer repo root after the template rename; propagation does not rewrite consumer-authored source.
- Added `devel/DEVEL_README.md`: per-file reference for the `devel/` maintenance scripts (version bump, PyPI submit, changelog lib/commit/query/rotate, broken-link flattener, dist clean), with a function-by-function table for each. Named `DEVEL_README.md` (matching `tests/TESTS_README.md`) to avoid confusion with the root `README.md`.
- Added meta test `tests/meta/test_merge_conftest.py` covering the block-aware `merge_conftest` behavior.
- Added `tests/meta/test_function_typing_behavior.py`: behavioral equivalence tests for `check_function_annotations` and `check_no_typing_import` in `tests/test_function_typing.py`; pins accept/reject behavior (missing annotation yields violation, bare type / parametrized generic / self+cls are clean, `from typing import` yields violation) so a later message-or-structure refactor cannot silently change the gate.
- Centralized the repo-root report-file path/purge/write/append logic into `file_utils.report_path` / `purge_report` / `write_report` / `append_report`, and migrated the six hygiene tests (ascii, bandit, pyflakes, markdown_links, shebangs, init_files) onto it.

### Behavior or Interface Changes

- Converted `tests/test_ascii_compliance.py` from a single aggregate test to the per-file harness
  shape: module-scope autouse `collect_report` fixture owns the fixer pass and populates
  `VIOLATIONS_BY_FILE`; `@pytest.mark.parametrize` drives `test_ascii_compliance[<rel>]` per file;
  `sync_report` replaced by `write_report_lines`; three-phase summary moved into
  `print_violation_summary` helper.

- Hygiene reports are now complete on every failure: each of the 12 refactored hygiene tests
  writes a full `report_<topic>.txt` for any violation count. The previous `SUMMARY_THRESHOLD`
  gate in `test_pyflakes_code_lint.py` (suppressed reports when fewer than 4 files were bad) is
  removed; a single bad file now produces a report. Syntax/parse errors are recorded as one
  `SyntaxError` entry per file instead of being swallowed by `return []` or causing an early
  raise without writing. The `raise AssertionError` / `pytest.fail(` pattern is replaced by
  plain `assert` in all 12 modules; the precompute fixture is the sole report-write site.

- `devel/commit_changelog.py` seed selection reworked to a diff-driven seam: `select_new_entries` now derives candidates from `git diff HEAD --unified=0` on `docs/CHANGELOG.md` via `get_diff_vs_head` and `parse_added_bullet_lines`, selecting only newly ADDED top-level `- ` bullet lines (per-hunk add-vs-edit classification via `min(removed_bullets, added_bullets)`); the `prior_sha` parameter is dropped from `select_new_entries`.
- `keep_recent_heading_run` retained as a safety boundary: keeps only the most recent run of consecutive day-block headings carrying added bullets, preventing stale date blocks from leaking into the commit-message seed.

- `reset_repo.py --dry-run` now previews real propagation actions: removed the dry-run short-circuit in `run_propagate` that printed a single placeholder line and returned; the function now always builds the context and calls `repolib.process.process_repo` with `dry_run` passed through, so planned file copies and directory creates are logged via `log_action` without mutating any files.
- Reduced `propagate_style_guides.py` to a small interactive single-repo tool: the CLI now exposes only `-n/--dry-run` and a required `-R/--repo` that takes a repo path (relative or absolute, e.g. `-R ../vosslab-skills` or `-R .`).
- Marker prediction and writing is now default single-repo behavior (formerly the `--write-marker` flag): dry-run predicts and logs marker changes, and a normal run writes them.
- `reset_repo.py` now owns bootstrap and calls the `repolib` package directly instead of shelling out to the entry script; it no longer depends on `propagate_style_guides.py`.
- `propagate merge_conftest` is now block-aware: it ensures BOTH the canonical `collect_ignore` block and a `REPO_HYGIENE_FILTERS` scaffold block reach consumer `tests/conftest.py` files, additively, without overwriting consumer-set values. Previously only `collect_ignore` was managed, so already-migrated consumers never received the `REPO_HYGIENE_FILTERS` scaffold.
- `tests/meta/conftest.py` now derives the repo root from `file_utils.get_repo_root()` (git) after a single unavoidable `__file__` bootstrap, instead of walking `__file__` up by hand.

### Fixes and Maintenance

- Migrated `tests/test_pyflakes_code_lint.py` (WP1) to the shared harness: replaced local `collect_violations` and `make_report_lines` with `file_utils.collect_file_violations` (chosen because pyflakes does its own parsing), `format_violation_report`, `format_violation_assert_message`, and `write_report_lines`; added `HEADER` constant and `clear_stale_reports()` call to fixture; switched parametrize `ids` from `lambda` to `file_utils.rel_id`; extracted thin `check_file(rel)` that resolves absolute path and invokes pyflakes; deleted local duplicates. Parity diff of `report_pyflakes_code_lint.txt` is identical; only `bad_file.py` fails.

- Migrated `tests/test_import_star.py` (WP1) to the shared harness: replaced local `collect_violations` and `make_report_lines` with `file_utils.collect_python_violations`, `format_violation_report`, `format_violation_assert_message`, and `write_report_lines`; added `clear_stale_reports()` call to fixture; switched parametrize `ids` from `lambda` to `file_utils.rel_id`; extracted thin `check_file(rel, tree)` combiner; deleted local duplicates.

- Refactored 7 per-file hygiene tests (`test_function_typing.py`, `test_pytest_hygiene.py`,
  `test_pyflakes_code_lint.py`, `test_import_dot.py`, `test_import_star.py`, `test_init_files.py`,
  `test_indentation.py`) to the precompute-fixture + per-file-assert shape: autouse module-scope
  `collect_report` fixture, `VIOLATIONS_BY_FILE` dict, `collect_violations` / `make_report_lines`
  helpers, `sync_report` call, no `raise AssertionError` or `pytest.fail(`.
- Converted 5 writer hygiene tests (`test_ascii_compliance.py`, `test_bandit_security.py`,
  `test_markdown_links.py`, `test_shebangs.py`, `test_import_requirements.py`) from
  `raise AssertionError` to plain `assert`, with report written via `sync_report`.
- Verified: clean baseline `pytest tests/` = 607 passed, no report files left; integration gate
  with staged 8-rule `bad_file.py` = exactly 8 failures and exactly 8 report files including
  `report_pyflakes_code_lint.txt` (previously missing).

- Fixed `devel/commit_changelog.py` bug where editing an OLD changelog bullet (e.g. a Markdown link fix under a past date) made that bullet appear "new" and pollute the commit-message seed; the diff-driven approach selects only genuinely added lines so past-date edits are invisible to the seed.
- Synced the `devel/commit_changelog.py` description in [docs/REPO_STYLE.md](REPO_STYLE.md) with the new diff-driven seed selection (was still describing the removed SHA-based, `(date, title)`-keyed mechanism).

- Fixed two missed self-references left from the `propagate/` -> `repolib/` rename: replaced `'propagate'` with `'repolib'` in `META_DIRS` in `repolib/model.py` (blocker: the walker would have descended into `repolib/` and routed the propagator package into consumer repos); changed `git_rm_recursive("propagate/", ...)` to `git_rm_recursive("repolib/", ...)` in `reset_repo.py` line ~466 (blocker: the old path no longer exists and would raise `CalledProcessError`, leaving `repolib/` behind in the consumer).
- Removed dead `ensure_git_perms()` function from `repolib/files.py`; no callers existed after the fix-permissions feature was dropped.
- Removed dead `resolve_repo_path()` function from `repolib/repo.py`; no production callers existed (both entry scripts pass paths directly into `repolib.process.build_context_for_repo`, which handles abspath/expanduser internally). Deleted `tests/meta/test_repolib_repo_discovery.py` whose four tests exercised only that function.
- Rewrote stale flag references in active code: `repolib/repo.py` error message no longer names `--source-dir`; `repolib/process.py` comment and log line no longer name `--bootstrap`; `reset_repo.py` module docstring and phase comment updated to describe direct repolib call instead of subprocess with `--bootstrap`.
- Updated `meta/docs/PROPAGATION_RULES.md` to name the `repolib/` package throughout: replaced four `propagate/model.py` references, updated `` `propagate/` helper package `` to `` `repolib/` helper package ``, and changed `test_propagate_` prefix to `test_repolib_` in the META_TEST_PREFIXES description.
- Added explanatory comment above `skip_confirm=False` constant in `repolib/process.py` `process_repo()`.
- Eliminated double `get_repo_root()` call in `tests/file_utils.py` `run_fixer_script()`: compute `root = get_repo_root()` once at the top of the function and use it for both `script_path` and `cwd=root`, saving one redundant `git rev-parse` subprocess per fixer call.
- Hardened `check_no_typing_import()` in `tests/test_function_typing.py` to catch submodule imports: `ast.Import` branch now flags `alias.name == "typing" or alias.name.startswith("typing.")` so `import typing.extensions` no longer slips through; `ast.ImportFrom` branch similarly catches `from typing.x import y` via `node.module.startswith("typing.")`.
- Backfilled `import pathlib` and `pathlib.Path` annotations on five `tmp_path` fixture params in `tests/meta/test_merge_conftest.py` so `test_function_typing` passes cleanly on the full suite.
- Backfilled builtin-style type annotations (PEP 604 unions, no `typing` module) across approximately 25 files (production code, meta tests, and vendored hygiene tests) so the new `tests/test_function_typing.py` guard passes on a clean run.
- Migrated `tests/test_import_dot.py`, `tests/test_import_star.py`, and `tests/test_import_requirements.py` to use `file_utils.iter_imports` instead of local AST-walk loops.
- Migrated `tests/test_ascii_compliance.py` and `tests/test_whitespace.py` to `file_utils.run_fixer_script`; adopted `rel_to_root` for parametrize ids and messages; removed the redundant `__file__` fallback in the `test_whitespace.py` run-fixer path.
- Backfilled complete type annotations (param types and return types) on every unannotated `def` in five meta test files: `tests/meta/test_propagate_helpers.py`, `tests/meta/test_should_ship_override.py`, `tests/meta/test_reset_repo_resolve_license.py`, `tests/meta/test_file_utils_discovery.py`, `tests/meta/test_detect_repo_type.py`. Used builtin generics (`list[str]`, `dict`) and PEP 604 unions (`str | None`); no `typing` module imported. Added `import pathlib` and `import pytest` where required for fixture type names (`pathlib.Path`, `pytest.MonkeyPatch`, `pytest.CaptureFixture`).
- Annotated the five remaining bare params in `tests/file_utils.py`: `iter_imports(tree)` -> `tree: ast.Module`; `collect_files` callables -> `gather_all_fn: collections.abc.Callable`, `gather_changed_fn: collections.abc.Callable`; `discover_files` optionals -> `extensions: collections.abc.Iterable | None`, `extra_filter: collections.abc.Callable | None`. Added `import collections.abc` in length-then-alphabetical order. Removed stale "No type annotation" claims from two docstrings.
- Completed Google-style docstrings (added Args + Returns sections) on three thin functions in `tests/file_utils.py`: `_split_null`, `list_tracked_files`, and `_gather_all_paths`.
- Corrected suite count in `docs/CHANGELOG.md` `2026-06-11` Developer Tests entry from `575 passed` to `574 passed` after env-var removal dropped one test.
- Removed unused `repo_root: str` parameter from `write_import_report` in `tests/test_import_requirements.py` and dropped the `REPO_ROOT` positional argument from both call sites; the function routes through the module `REPORT_NAME` constant and `file_utils.write_report`, so `repo_root` was never used in the body.
- Extracted `record_indentation_report(details: list[str]) -> str` helper in `tests/test_indentation.py`, replacing two identical inline first-creation-header + append blocks; added import-group heading comments (`# Standard Library`, `# PIP3 modules`, `# local repo modules`); wrapped the `FILES` list comprehension under 100 chars.
- Added one blank line between the comment block and the `#====` separator at the top of `_keep_top_level_test` in `tests/test_pytest_hygiene.py` so the separator reads cleanly.
- Added `reset_pyflakes_report()` autouse module-scope fixture to `tests/test_pyflakes_code_lint.py` that calls `file_utils.purge_report(REPORT_NAME)` before each run, consistent with `test_bandit_security.py` and `test_init_files.py`.
- Wrapped the 101-char `record_naming_violations(...)` call in `tests/test_test_naming_conventions.py` under 100 chars by splitting the argument list across two lines.
- Rewrote the `tests/test_pytest_hygiene.py` guard description in both `docs/PYTEST_STYLE.md` and `tests/TESTS_README.md` positively: "keeping all file-discovery logic in `file_utils`" replaces the negative "do not reintroduce" phrasing, following the "prompt positively" principle.

### Removals and Deprecations

- Removed `file_utils.append_report_block` and `file_utils.append_report` after migrating `tests/test_test_naming_conventions.py` to the precompute-fixture + `sync_report` pattern (the last caller).

- Removed dead functions from `devel/commit_changelog.py`: `compute_new_entries`, `get_changelog_text_at`, and `get_last_changelog_commit_sha` are deleted; their role is now handled entirely by the diff-driven `added_changelog_bullet_lines` path.

- Removed `propagate_style_guides.py` CLI flags `--base-dir`, `--source-dir`, `--bootstrap`, `--no-auto-discover`, `--fix-permissions`, `--write-marker`, `--yes`, and `-v/--verbose-paths`.
- Removed batch multi-repo propagation (the no-`-R` "update every repo under ~/nsh" mode), including `collect_repo_dirs`, `resolve_target_repos`, and the skip-list.
- Dropped `PropagateContext` fields `base_dir`, `fix_permissions`, `skip_confirm`, and `verbose_paths`.
- Removed the unused invented env vars `REPO_HYGIENE_SCOPE` / `FAST_REPO_HYGIENE` / `SKIP_REPO_HYGIENE` and the changed-scope/skip discovery path (`resolve_scope`, `collect_files`, `list_changed_files`, `_gather_changed_paths`) from `tests/file_utils.py`; `discover_files` now always scans all tracked files.
- Deleted dead function `repo_hygiene_excludes(test_key, rel)` from `tests/file_utils.py`; it had zero callers -- `discover_files` inlines the same registry-load and fnmatch pattern logic directly.
- Removed fragile meta tests: deleted `tests/meta/test_repolib_plan_matches_legacy.py` (required-key-list asserts that broke on any manifest change); removed the trivial `exit_code_for` tests and the hardcoded-token-loop test from `tests/meta/test_repolib_helpers.py`; pruned fragile assertions from `tests/meta/test_propagate_cli.py` (inspect.getsource source-string assertion, sys.argv-mutating argparse tests, duplicate path test, `__file__`-pinned source_dir assertion), keeping one robust source-vs-target test.

### Decisions and Failures

- Renamed the `propagate/` package to `repolib/` to reflect that it is a shared repo-operations library used by multiple entry scripts, not a single-tool detail.
- Source template root now resolves from the running checkout that contains `repolib/` (anchored on the package `__file__`), never from the target `-R` repo, so propagation cannot copy from the wrong repo.
- Require-annotations is enforced repo-wide as a hard test. The repo accepts that downstream consumer repos must backfill their own annotations; keeping every consumer green in lockstep was explicitly ruled out of scope.
- `collections.abc.Callable` and `collections.abc.Iterable` are used for callable and iterable params. `collections.abc` is allowed; the `typing` module is not.
- The single irreducible `__file__` use in `tests/meta/conftest.py` is the import bootstrap; every other path in that file derives from `get_repo_root()`.
- The env knobs `REPO_HYGIENE_SCOPE`, `FAST_REPO_HYGIENE`, and `SKIP_REPO_HYGIENE` were never set in real runs and violated the `docs/PYTHON_STYLE.md` no-invented-env-vars rule; removing them is a behavior-preserving simplification because real runs always used the "all" scope.

### Developer Tests and Notes

- Added `report_name(test_file: str) -> str` and `append_report_block(name: str, header: str, lines: list[str]) -> str` to `tests/file_utils.py`. `report_name` derives the canonical `report_<stem>.txt` filename from a test module path (`__file__`), eliminating hardcoded report names. `append_report_block` owns the header-on-first-creation plus line-append pattern used by parametrized hygiene tests. Covered by new meta tests in `tests/meta/`.
- Migrated all 13 hygiene tests to derive `REPORT_NAME = file_utils.report_name(__file__)` instead of hardcoding. Five report files were renamed to match their test filenames: `report_bandit.txt` -> `report_bandit_security.txt`, `report_init.txt` -> `report_init_files.txt`, `report_pyflakes.txt` -> `report_pyflakes_code_lint.txt`, `report_shebang.txt` -> `report_shebangs.txt`, `report_test_naming.txt` -> `report_test_naming_conventions.txt`. Append-style tests (function_typing, import_dot, import_star, indentation, init_files, pytest_hygiene, test_naming_conventions) now emit through `append_report_block`; write-style tests (ascii_compliance, bandit_security, import_requirements, markdown_links, pyflakes_code_lint, shebangs) continue to use `write_report`.
- Added `tests/meta/test_function_typing_behavior.py` pinning the accept/reject equivalence of `check_function_annotations` and `check_no_typing_import` before and after the `tests/test_function_typing.py` decomposition. Rewrote failure messages in `test_function_typing.py` to state the preferred form positively (plain builtin type is enough, e.g. `-> list` or `-> None`); decomposed the check functions into small single-purpose units while keeping the `(tree, rel) -> list[str]` signatures stable.
- Renamed meta tests `test_propagate_*.py` to `test_repolib_*.py`; removed tests for the deleted batch discovery; added `tests/meta/test_propagate_cli.py` covering the argparse surface (only `-h`/`-n`/`-R`, `-R` required), relative/absolute path resolution, source-vs-target separation, and that `reset_repo.py` bootstraps via `repolib.process` with no subprocess. Full suite after this refactor: 584 passed.
- Full suite `source source_me.sh && pytest tests/` -> 574 passed.
- Updated `docs/REPO_STYLE.md` Project type marker section: replaced `propagate/` package name with `repolib/`, expanded submodule references to `repolib.repo.read_repo_type` and `repolib.files.compute_propagation_plan`, and added a clause noting that `reset_repo.py` now calls `repolib` directly. Tightened `apply_file_bucket` return annotation from bare `tuple` to `tuple[int, int, int]`. Confirmed `dest='dry_run'` already present in `propagate_style_guides.py` parse_args (no edit needed). Full suite: 589 passed.
- Documentation audit fixes applied to `docs/CHANGELOG.md`, `docs/PYTEST_STYLE.md`, `docs/PYTHON_STYLE.md`, and `tests/TESTS_README.md`: corrected suite count (555->575), updated stale `git_file_utils` references to `file_utils`, cleaned `NOEXIST` parenthetical artifact, rewrote hygiene-section negative phrasing positively, documented three new `file_utils.py` helpers and two new hygiene guard tests, and refreshed `## TYPE HINTING` to reflect enforcement by `tests/test_function_typing.py`.

## 2026-06-10

### Additions and New Features

- Added shared hygiene file-discovery API `file_utils.discover_files(extensions=None, extra_filter=None, *, test_key=None, repo_root=None)` to `tests/file_utils.py`. `test_key` and `repo_root` are keyword-only (bare `*` in signature). Owns the full discovery pipeline: scope selection, absolute-path join, deduplication, skip-dir filtering, case-insensitive extension filtering, per-test `extra_filter` injection, and Layer 2 repo-hygiene-filter application. Returns sorted absolute paths; `extra_filter` and Layer 2 patterns receive a repo-relative POSIX path. Documented in `docs/PYTEST_STYLE.md` and `tests/TESTS_README.md` (ships as NOEXIST: seeded once at bootstrap and not overwritten by subsequent propagation syncs).
- Added single `SKIP_DIRS` frozenset to `tests/file_utils.py` (`.git`, `.venv`, `__pycache__`, `.pytest_cache`, `.mypy_cache`, `old_shell_folder`, `legacy`), replacing four diverged per-test variants. Segment-match only: `legacy/foo.py` is skipped, `notlegacy/foo.py` is kept. Separator-normalized so git-style and OS-style paths behave identically.
- Added shared `path_has_skip_dir(path: str) -> bool` to `tests/file_utils.py` with normalized separator and per-segment matching, replacing three drifted per-test signatures.
- Added `REPO_HYGIENE_FILTERS` dict to `tests/conftest.py` as the repo-local Layer 2 exclusion registry. Shape: `dict[str, list[str]]` where keys are `"all"` or a vendored test key (test filename stem with `test_` removed), and values are lists of repo-relative POSIX glob patterns matched via `fnmatch.fnmatchcase`. An `"all"` pattern excludes a file from every test; a test-key pattern applies only when that `test_key` is passed to `discover_files`. Recursive subtree exclusion requires explicit `/**` suffix. This template ships with an empty registry (`REPO_HYGIENE_FILTERS = {}`).
- Added three-layer exclusion model to `file_utils.discover_files`: Layer 1 `SKIP_DIRS` (vendored, universal directories); Layer 2 `REPO_HYGIENE_FILTERS` (repo-local, per-test glob exclusions in `tests/conftest.py`, the only home for repo-specific data because `conftest.py` is merge-preserved by propagation while all vendored files are overwritten); Layer 3 `extra_filter` at the vendored call site (universal per-test selection only, never repo-specific exclusions).

### Fixes and Maintenance

- Relocated `tests/test_file_utils_discovery.py` to `tests/meta/test_file_utils_discovery.py` using `git mv`. Added `sys.path.insert` header matching the existing meta-test pattern so `import file_utils` resolves from `tests/` when collected from the `tests/meta/` subdirectory.
- Migrated all 11 vendored hygiene tests to `file_utils.discover_files` with `test_key=<stem>`. Removed local `SKIP_DIRS`, `path_has_skip_dir`, `filter_py_files`, `gather_files`, `gather_changed_files` from every test. Module-level discovered list renamed from `_FILES` to `FILES` in all 11 tests. Old vs. new selected-file counts matched exactly across all tests (51 `.py` for pyflakes/import tests; 105 all-files for shebangs; 92 whitespace; 50 bandit/indentation; 1 init_files; 93 ascii; 27 markdown).
- `test_indentation.py` now relies on shared `SKIP_DIRS` for `old_shell_folder` exclusion instead of a raw string check. `test_init_files.py` uses `extra_filter=_keep_init_py` to select by basename.
- Removed inline `SKIP_FILES`, `SKIP_FILE_PATTERNS`, and `_ascii_keep` machinery from `test_ascii_compliance.py`. Repo-specific exclusions that were baked into that function now belong in `tests/conftest.py REPO_HYGIENE_FILTERS`. This template had no single-repo exclusions to transfer; the inline functions were removed entirely, not migrated.

### Removals and Deprecations

- Dropped `_FILES` module-level naming in all 11 vendored hygiene tests; replaced with `FILES` (no leading underscore). The underscore form is deprecated: `docs/PYTEST_STYLE.md` now documents `FILES` as the canonical name.
- Dropped a former template-specific exclusion that was present in five `.py` hygiene tests (matched no tracked file in this repo). Repo-specific exclusions now belong in `tests/conftest.py REPO_HYGIENE_FILTERS` only; vendored files never hold single-repo data.

### Decisions and Failures

- Centralized discovery in `tests/file_utils.py` (not an env var, which violates `docs/PYTHON_STYLE.md`; not `conftest.py`, which is auto-loaded rather than importable by name). Redesign of the full scaffold, not just the skip constant, so the only per-test inputs are extensions, `extra_filter`, and `test_key`; everything invariant (scope, skip-dirs, dedup, isfile) has one use site.
- `REPO_HYGIENE_FILTERS` lives in `tests/conftest.py` because that is the only file in the test tree that propagation handles via merge (inject `collect_ignore` only, preserve the rest). All vendored files (including `file_utils.py` and every `tests/test_*.py`) are overwritten by propagation, so they must hold no repo-specific data. Putting exclusions in `conftest.py` is the only home that survives a propagation sync.
- `extra_filter` is a universal per-test SELECTION mechanism (keep only `__init__.py`), not a home for repo-specific exclusions. Repo-specific exclusions that used to live inside `_ascii_keep` or similar inline functions were moved to `REPO_HYGIENE_FILTERS` in `conftest.py`. The two injection points are now cleanly separated: `extra_filter` = "which shape of files does this test care about"; `REPO_HYGIENE_FILTERS` = "which specific files does this repo want excluded".
- `SKIP_DIRS` is a superset union including `legacy` (preventative): every entry is a build, cache, or legacy directory no hygiene scan should lint. Segment-match prevents false positives on names like `notlegacy/`.
- Propagation atomicity confirmed: `file_utils.py` and all 11 `test_*.py` files route to the `test_files` bucket (universal walker, no `ROUTING_OVERRIDES` gates on language or requirement); `tests/conftest.py` routes through `merge_conftest` (injects `collect_ignore`, preserves `REPO_HYGIENE_FILTERS` and all other consumer content). Both applied in a single `process_repo()` pass with no version-skew window.

### Developer Tests and Notes

- Regression test `tests/meta/test_file_utils_discovery.py` (template-internal; not shipped to consumers): 11 cases cover segment-match, separator normalization, scope, extensions, `extra_filter` contract, and Layer 2 `REPO_HYGIENE_FILTERS` behavior (all-key, per-key match, per-key non-match, recursive-glob subtree). Uses `monkeypatch` for deterministic gather functions, stubbed registry loader, and `tmp_path` for offline isolation under one second.
- Guard test `tests/meta/test_repo_hygiene_filters.py` (template-internal; not shipped): 4 cases validate the `REPO_HYGIENE_FILTERS` contract on the live `tests/conftest.py`: must be a dict; every key must be `"all"` or match a `tests/test_<key>.py` file; every value must be a list of strings; empty registry passes. Guards downstream repos against stale or mistyped keys that would silently do nothing.
- Propagation dry-run (`propagate_style_guides.py --dry-run --repo energy` for `python` type; `--dry-run --repo junk-drawer` for `python`/`other` type): all 11 migrated tests plus `tests/file_utils.py` appear as `dry run copy/update` entries for both repo types. `tests/conftest.py` does not appear in the dry-run file list (handled separately via `merge_conftest`, which injects only the `collect_ignore` block and preserves `REPO_HYGIENE_FILTERS`). `tests/meta/` excluded from propagation because `meta` is in `META_DIRS` (defense-in-depth: also in `SKIP_WALK_DIRS`); template-internal only.
- Routing confirmed for all four `REPO_TYPE` tokens (`python`, `typescript`, `rust`, `other`) by code inspection: the universal walker at `propagate/files.py` covers all five tokens; no `ROUTING_OVERRIDES` entry gates any of the 12 vendored files by language or requirement.
- Full suite green: `pytest tests/` 476 passed (472 prior + 4 new guard test functions).

## 2026-06-03

### Additions and New Features

- Added per-destination-repo exclusion to `ROUTING_OVERRIDES` (`propagate/model.py`) via a new optional `exclude_repos` field (a set/frozenset of repo basenames). `propagate.files.should_ship_override` now returns `False` when `os.path.basename(os.path.normpath(repo_dir))` is in that set, so a file can be blocked from shipping to specific destination repos. First and only use: `docs/CLAUDE_HOOK_USAGE_GUIDE.md` is excluded from `claude-code-permissions-hook`, which is the canonical source of that doc; the mirror must never overwrite the source of truth.

### Decisions and Failures

- Reused the existing `ROUTING_OVERRIDES` table for the exclusion rather than a one-off `if` branch or a new file-based config. Keeps the special case data-driven and co-located with the existing language/requirement gates; user confirmed a hard-coded special case (not a config file) was acceptable.

### Developer Tests and Notes

- Added `TestShouldShipOverrideExcludeRepos` to `tests/meta/test_should_ship_override.py`: blocked for `claude-code-permissions-hook`, ships to other repos, and trailing-slash on `repo_dir` does not defeat the exclusion (covered by `os.path.normpath`). Extended the schema guardrail to validate `exclude_repos` is a non-empty set/frozenset/list/tuple. Gates green: `pytest tests/meta/` 145 passed.

## 2026-05-28

### Additions and New Features

- New `propagate.files.merge_at_imports_safe(source, dest, dry_run, counters)` (in `propagate/files.py`) implements set-union merge for flat `@`-import list files. Algorithm: dest-missing -> copy template verbatim (`created`); else parse both files line by line, drop consumer lines matching the user-editable strip list at `meta/propagation/deprecated_claude_md.txt`, append any template `@`-import not already present (set semantics, no duplicates) after the last surviving `@`-line in the consumer (or at top after leading blanks if none), preserve consumer non-`@` content and trailing-newline state. Outcomes `created` / `merged` / `unchanged` / `error` share the existing `merged_count` and `created_count` counters. The `error` outcome reduces to "source file missing"; consumer state is always acceptable (fenced or plain, with or without consumer-local imports).
- New user-editable strip manifest `meta/propagation/deprecated_claude_md.txt`. Same loader (`propagate.files.load_deprecation_list`) and same comment-and-blank-line semantics as the existing `deprecated_gitignore.txt` and `deprecated_tests.txt` manifests; every non-comment line is stripped from every consumer `CLAUDE.md` on every sync without per-line origin check. Seeded with the two fence-marker comment lines (`<!-- === TEMPLATE-MANAGED START === -->` and `<!-- === TEMPLATE-MANAGED END === -->`) so the 3 consumer repos still carrying the retired fenced-merge convention get cleaned automatically on next sync. Future retired `@`-imports added one line at a time, justified in this changelog.

### Behavior or Interface Changes

- `CLAUDE.md` migrated from fenced-region MERGE to set-union MERGE. Template `CLAUDE.md` no longer carries the `<!-- === TEMPLATE-MANAGED START === -->` / `END` fence comments; ships as a plain six-line `@`-import list (`@AGENTS.md` + five `docs/*.md`). The MERGE dispatcher at `propagate_style_guides.py:293` now routes by filename: `file_rel == 'CLAUDE.md'` -> `merge_at_imports_safe`; everything else -> existing fenced `merge_file_safe`. Fence-marker requirement on consumer `CLAUDE.md` is gone -- existing consumer repos (44+ on this user's machine, surveyed via `cat $(find . -name CLAUDE.md) | sort | uniq -c`) sync cleanly without hand-editing. The fenced-merge code path is preserved for future MERGE files; no other file currently uses MERGE.
- `reset_repo.py` git-rm cleanup phase generalized: replaced the single hard-coded `git_rm_recursive("meta/", ...)` call with a walk of the git index that strips every directory named `meta/` at any depth. Walks `git ls-files`, finds the shallowest `meta` component in each tracked path, deduplicates so a child `tests/meta/` under an ancestor `meta/` is dropped from the strip list, then issues one `git rm -r <path>` per surviving entry. Covers top-level `meta/`, `tests/meta/`, and any future `<subtree>/meta/` without per-path maintenance. Verified via `reset_repo.py --dry-run --type python --non-interactive --yes`: output now contains both `git rm -r meta/` and `git rm -r tests/meta/`.

### Fixes and Maintenance

- Consumer repos no longer inherit ~15 template-meta pytest files (`tests/meta/test_propagate_*`, `tests/meta/test_merge_bucket_*`, `tests/meta/test_no_meta_leaks.py`, etc.) at bootstrap. These tests imported the now-removed `propagate/` package and the now-removed `propagate_style_guides.py` entry script; running `pytest tests/` in a fresh consumer would surface collection-time `ModuleNotFoundError` failures with no actionable fix on the consumer side. Root cause: `reset_repo.py` already stripped the top-level `meta/` source tree but missed `tests/meta/`. The generalized walk closes the gap and is forward-compatible with any future `<subtree>/meta/` placement.
- Updated `meta/docs/MERGE_BUCKET_SPEC.md` to document the two-helper dispatch model: new "Set-union merge (`@`-import list files)" section ahead of the existing fenced-region section; algorithm summary; outcome table; strip-list semantics. Reworded the "Bucket dispatch summary" section to reflect the filename-based routing (`CLAUDE.md` -> `merge_at_imports_safe`; everything else -> `merge_file_safe`). The "Precedent" entry for `CLAUDE.md` rewritten to record the fence-to-set-union migration and the strip-list seeding.

### Removals and Deprecations

- Removed the four-line `<!-- === TEMPLATE-MANAGED START === -->` / `END` fence wrapper from template `CLAUDE.md`. The six `@`-import lines are now the entire file body. The fence-marker lines themselves are tracked in `meta/propagation/deprecated_claude_md.txt` so consumer files that still carry them get cleaned on next sync.

### Decisions and Failures

- Rejected OVERWRITE for `CLAUDE.md`. Cross-repo survey (47 consumer repos) found 6 with repo-specific `@docs/PRIMARY_*.md` imports, 7 with `@docs/TYPESCRIPT_STYLE.md`, 9 with one-off project-specific imports (`@docs/EMWY_YAML_v2_SPEC.md`, `@docs/EPCUBE_API_FIELDS.md`, etc.). OVERWRITE would wipe every one of those on next sync.
- Rejected NOEXIST for `CLAUDE.md`. NOEXIST seeds once and never updates the consumer thereafter, so a future template addition (`@docs/NEW_STYLE.md`) would not propagate to any of the 47 existing consumers. The user adds `@`-imports to the template to push them to every consumer; NOEXIST defeats that intent.
- Rejected auto-fence-on-first-sync for `CLAUDE.md`. The propagator would have to guess where the template-managed region ends inside the consumer's existing content; any heuristic could silently lose consumer additions outside the guessed region. Set-union has no such failure mode -- every consumer line is either preserved or matched against the explicit strip list.
- Selected set-union with explicit strip list. Adds are driven by template content (additive merge, no fences required); removes are driven by the user-editable `meta/propagation/deprecated_claude_md.txt` (subtractive, explicit). The two mechanisms are independent: a renamed docs file requires one strip-list addition plus one template edit. Strip list grows over time and never shrinks; a retired `@`-import stays retired across all consumers.
- Folder-name convention for `deprecated_claude_md.txt` follows the established `meta/propagation/deprecated_*.txt` pattern (lowercase snake_case for non-Markdown files per `docs/REPO_STYLE.md`), not SCREAMING_SNAKE_CASE which is reserved for `.md` docs.

### Removals and Deprecations (continued)

- Deleted fenced-region MERGE infrastructure: `merge_file_safe`, `_find_fences`, and `FENCE_STYLES` in `propagate/files.py` (~140 lines); the `if file_rel == 'CLAUDE.md'` branch in `propagate_style_guides.py:apply_file_bucket` (dispatcher collapsed to a single direct call to `merge_at_imports_safe`); and the two fenced-merge test files (`tests/meta/test_merge_bucket_roundtrip.py`, `tests/meta/test_merge_bucket_error_cases.py`, ~300 lines combined). The fenced-region code path had zero production users after the CLAUDE.md set-union migration -- `MERGE_FILES` contains a single entry (`CLAUDE.md`) which routes through the set-union helper, leaving the fenced path structurally unreachable. Per `docs/REPO_STYLE.md` "do not design for hypothetical future requirements." If a future file needs fenced merge, restore the helper from git history at that time; the test fixtures show exactly how it was called.
- Rewrote `meta/docs/MERGE_BUCKET_SPEC.md` and updated `meta/docs/PROPAGATION_RULES.md` to drop the fenced-region algorithm, fence convention table, error-case table, and migration section. Spec is now ~30 lines (was ~107); describes only the set-union algorithm that exists.

### Developer Tests and Notes

- New `tests/meta/test_merge_at_imports_roundtrip.py` (9 cases after audit pruning + two added): `created` when dest missing, `unchanged` when consumer has every template entry, `merged` when consumer missing one template entry (assert added line + consumer-local entry preserved), plain-shape-success (no fences required), dedup (template re-listing not duplicated), deprecation-strip (lines in monkeypatched strip list removed from consumer), dry-run-does-not-modify. Strip list mocked via `monkeypatch.setattr(propagate.files, '_load_claude_md_deprecated', lambda: [...])` so tests are deterministic and decoupled from the real manifest file content.
- Existing fenced-merge tests (`tests/meta/test_merge_bucket_roundtrip.py`, `tests/meta/test_merge_bucket_error_cases.py`) require no edits: both use synthetic templates, not `CLAUDE.md` directly, so the fence-error invariants still pin valid behavior of the unchanged `merge_file_safe` helper. The `test_error_consumer_plain_shape_no_fences` case still holds: a future fenced-merge file with a plain consumer is still an error; the rule only relaxed for `CLAUDE.md` because the dispatcher routes it to the set-union helper before fence parsing.
- Gates green: `pytest tests/` 464 passed (up from 457); `pyflakes propagate/files.py propagate/model.py reset_repo.py propagate_style_guides.py tests/meta/test_merge_at_imports_roundtrip.py` clean; smoke `python3 reset_repo.py --dry-run --type python --non-interactive --yes --code-license mit --docs-license cc-by-sa` lists both `git rm -r meta/` and `git rm -r tests/meta/` in its 16-action plan.

## 2026-05-24

### Additions and New Features

- New `templates/typescript/.prettierrc` documents the formatter choice explicitly (`useTabs: false`, `tabWidth: 2`, `printWidth: 100`). Codifies the previously-implicit prettier default so a future contributor cannot reintroduce tabs and assume "prettier default" is open to debate. Ships OVERWRITE so consumers track the upstream convention.
- New `templates/typescript/.prettierignore` excludes generated and lock trees (`node_modules/`, `dist/`, `dist-single/`, `generated/`, `_site/`, `coverage/`, `playwright-report/`, `test-results/`, `blob-report/`, `package-lock.json`). Prettier scope is intentionally narrow: only `.js .ts .mjs .cjs .tsx .mts .cts` are prettier-managed; JSON/YAML/MD/Python are explicitly out of scope.
- New `templates/typescript/noexist/tsconfig.lint.json` extends `./tsconfig.json` with `include: ["tests/**/*.ts", "tools/**/*.ts"]`. Ships via NOEXIST so consumers can extend the include list locally without losing the file on next propagation sync. Consumed by `check_codebase.sh` step 2 (`typecheck:lint`) and pinned in `templates/typescript/eslint.config.js` `parserOptions.project` so typescript-eslint has type info for `.ts` files outside `src/`.
- Added `format:write` script to `templates/typescript/noexist/package.json` mirroring `format:check` with `--write` (`npx prettier --write '**/*.{ts,tsx,mts,cts,js,mjs,cjs}'`). One-command auto-fix path when `format:check` fails.

### Behavior or Interface Changes

- `templates/typescript/check_codebase.sh` step 2 (`typecheck:lint`) no longer SKIPs when `tsconfig.lint.json` is absent. Default `tsconfig.lint.json` now ships via NOEXIST, so the conditional was masking a missing-file design flaw. Step is now unconditional.
- `templates/typescript/check_codebase.sh` step 3 (`lint`) dropped `--no-error-on-unmatched-pattern` and replaced the narrow `'src/**/*.ts' 'tests/**/*.ts' '*.ts'` glob with the single recursive glob `'**/*.{ts,tsx,mts,cts,js,mjs,cjs}'`. Repos with deeper layouts (e.g. `packages/*/src/`) and any `.mjs/.cjs/.js` files now actually get linted; empty-match repos now fail loudly instead of silently passing. Same glob shape as step 4 (`format:check`) for one-to-one parity.
- `templates/typescript/check_codebase.sh` step 6 (`test:node`) now guards on `compgen -G 'tests/test_*.mjs'`. A consumer with no `.mjs` tests emits a loud SKIP instead of the prior silent `node --test` no-op.
- Replaced `templates/typescript/check_codebase.sh` step 2 comment that claimed `tsc` exits 0 on empty includes -- it does not; `tsc -p tsconfig.lint.json` exits 2 with `TS18003` ("No inputs were found in config file"). New comment documents the real behavior and the two consumer workarounds (seed a stub `.ts` in `tests/` or `tools/`, or edit the consumer-owned `tsconfig.lint.json` to narrow `include`). Same correction landed in `templates/typescript/docs/TYPESCRIPT_STYLE.md` empty-tree paragraph.
- Simplified the propagation dotfile filter in `propagate/files.py`: removed the `(name.startswith('.') and not name.endswith('.json'))` skip block AND removed the `OVERLAY_DOTFILE_ALLOWLIST` carve-out from `propagate/model.py`. Replaced the two-stage skip-then-carve-out with the principle "every file under `templates/<type>/` ships; the META filter already excludes template-internal names." Verified zero `.gitkeep` files exist anywhere in the repo, so the original `.gitkeep` denylist was guarding against nothing.
- `templates/typescript/eslint.config.js` upgraded `@typescript-eslint/explicit-function-return-type` from `warn` to `error`. The `warn` label was dead documentation: `--max-warnings 0` in the gate already promoted every warning to a hard failure. Severity field now matches reality.
- `templates/typescript/eslint.config.js` split into two `files:` blocks. Typed-rules (`tseslint.configs.recommendedTypeChecked`) apply only to `.ts/.tsx/.mts/.cts`; `.js/.mjs/.cjs` files (eslint config itself, playwright `.mjs` helpers, tools `.mjs` CLIs) use `tseslint.configs.disableTypeChecked` so typed rules do not error on files outside any tsconfig project.
- `templates/typescript/noexist/tsconfig.json` no longer sets `exactOptionalPropertyTypes: true`. Strict-light becomes the default. Most third-party `@types/*` packages fail under EOPT even with `skipLibCheck`; the friction was higher than the strictness payoff for new repos. Consumers who want it can re-add the flag locally (`tsconfig.json` is NOEXIST and consumer-owned after bootstrap).
- Updated `devel/bump_version.py` for the common CalVer workflow. `--set-version 26.05` now works when no version source exists by creating a root `VERSION` entry, and an existing but empty `VERSION` file is updated instead of being ignored. No-argument runs now default to the current `YY.MM` CalVer value, so `--apply` alone can refresh/create `VERSION` for the current month. Added `-c` / `--calver` to request the current CalVer value explicitly. Added single-letter conveniences: positional bump shortcuts `M`/`m`/`p`/`a`/`b`/`r` for major/minor/patch/alpha/beta/rc, `-v` for `--set-version`, `-A` for `--apply`, and `-u` for `--update-all`. Minimized normal `--help` per `docs/PYTHON_STYLE.md` argparse guidance so the visible surface focuses on CalVer, explicit version, and apply; advanced/testing flags remain accepted and can be shown with `--help-advanced`. Re-running the same version is now an idempotent no-op instead of a nonzero error.

### Fixes and Maintenance

- Reformatted `templates/typescript/eslint.config.js`, `templates/typescript/tests/playwright/repo_root.mjs`, and `templates/typescript/tools/html_to_pdf.mjs` from tabs to two-space indent. The template-shipped files now pass the template-shipped `npx prettier --check`. Previously every fresh consumer failed `format:check` on day one because prettier's built-in default is two-space and the hand-authored files used tabs.
- Extended `tests/meta/test_propagate_plan_matches_legacy.py` expected typescript-overlay sets to cover the three new propagation entries: `.prettierrc` and `.prettierignore` under OVERWRITE, `tsconfig.lint.json` under NOEXIST. Manifest pinning stays accurate to the propagation plan; `pytest tests/meta/` stays green.
- Rewrote `templates/typescript/tests/TESTS_README.md` after the pytest sweep (see Removals). Removed the per-file table for the six deleted tests, removed the stale `bash check_codebase.sh --fast` invocation (the `--fast` flag was retired in the 2026-05-22 self-contained rewrite), and added a short "Resist adding Python tests for JS/TS ecosystem checks" note pointing future contributors to `check_codebase.sh` instead. File ships NOEXIST so existing consumers keep their current copy until next `--bootstrap`.
- Edited `README.md` Quick start to drop `bash tests/e2e/run_all.sh` (no such runner exists in this repo). Replaced with a one-sentence pointer to [E2E_TESTS.md](E2E_TESTS.md) and the per-runner invocation pattern `bash tests/e2e/e2e_<name>.sh`.
- Updated `templates/typescript/docs/TYPESCRIPT_STYLE.md` to document the two-space JS/TS rule (with explicit contrast to `docs/PYTHON_STYLE.md` tabs), the `.prettierignore` scope, the `npm run format:write` auto-fix path, the `explicit-function-return-type: error` rationale, and a new "Opt-in strict flags (not default)" section covering `exactOptionalPropertyTypes` and `tsconfig.lint.json`. Removed the EOPT row from the tsconfig defaults table.

### Removals and Deprecations

- Deleted six pytest/mjs files under `templates/typescript/tests/` that routed JS/TS-ecosystem verification through the wrong tool. Each one either ran `npx` via subprocess inside the pytest fast lane ([docs/PYTEST_STYLE.md](PYTEST_STYLE.md) violation: real CLI round-trip), asserted hardcoded tsconfig/package.json defaults that duplicated the configs themselves, or stubbed a check that `check_codebase.sh` already runs natively. The TS ecosystem owns its own gates: `tsc`, `eslint`, `prettier`, `node --test`, all wired through `check_codebase.sh`. Removed files:
  - `test_typescript_eslint.py` (subprocess `npx eslint` mirror of `check_codebase.sh` step 3).
  - `test_typescript_tsc.py` (subprocess `npx tsc` mirror of `check_codebase.sh` step 1).
  - `test_tsconfig_canonical.py` (16 tests asserting hardcoded compiler-flag defaults; tsconfig schema duplication; `tsc` is the real correctness gate).
  - `test_package_json_schema.py` (2 of 4 tests asserted hardcoded required-key lists for scripts and devdeps; invalid JSON / missing keys would fail `npm install` loudly anyway).
  - `test_eslint_config_present.py` (asserted `eslint.config.js` exists and lists certain devdeps; `check_codebase.sh` step 3 fails loudly without a working eslint config).
  - `test_smoke.mjs` (no-op stub; not pytest-shaped; only existed to keep `node --test` from erroring on empty test sets, now handled by the `compgen -G` SKIP guard in step 6).
- Removed `"exactOptionalPropertyTypes": true` from `templates/typescript/noexist/tsconfig.json` (see Behavior or Interface Changes above for rationale).

### Decisions and Failures

- Rejected `useTabs: true` in `.prettierrc` as an alternative to reformatting the four tab-indented files. That direction would force a reformat of every consumer `src/*.ts` already authored under prettier's two-space default and invert the JS/TS industry convention. Aligning the template to its existing `src/*.ts` (spaces) is the lower-friction path; the new `.prettierrc` codifies the prior implicit choice so it is grep-able instead of folklore.
- Decided `exactOptionalPropertyTypes` strict-light default over shipping a `tsconfig.strict.json` variant. Single canonical default keeps consumers from drifting between two strictness levels; the opt-in path is documented in `templates/typescript/docs/TYPESCRIPT_STYLE.md` for consumers who want it back.
- Plan's WS-SELFGATE workstream (a `tests/e2e/e2e_typescript_template_self_check.sh` runner) was prototyped, verified passing, then **deleted** at user request. Reason: surface area was too wide for a manual canary (network `npm install`, tmp-dir cleanup, propagator invocation with `--bootstrap`, `sed -i` placeholder substitution duplicating `reset_repo.py`, and `.ts` stub seeding that masked a real consumer failure mode rather than surfacing it). Replacement verification: run `bash check_codebase.sh` inside a real downstream typescript consumer after propagation. Side finding from the prototype (kept for the record): `npx tsc --noEmit -p tsconfig.lint.json` errors `TS18003` and exits 2 when its `include` matches no files -- documented in `templates/typescript/docs/TYPESCRIPT_STYLE.md` and in the corrected step 2 comment in `check_codebase.sh`.
- Replaced the planned `OVERLAY_DOTFILE_ALLOWLIST` (hardcoded `frozenset({'.prettierrc', '.prettierignore'})`) with no allowlist at all. Hardcoded set was already the second carve-out around a denylist of one (`.gitkeep`) that guards against zero files in the repo. Inverted the design: ship every file under `templates/<type>/` by default; the META filter already handles exclusions. Rejected alternatives: a sentinel-marker convention (`<name>.propagate-as`) was overkill, and a small `OVERLAY_DOTFILE_DENYLIST` would just inherit the same maintenance problem one polarity over.
- Rejected adding pytest mirrors for the TS toolchain. "Python and typescript are different" -- pytest is the Python test runner; routing JS/TS-ecosystem shape assertions through it (tsconfig defaults, package.json keys, eslint config presence) used the wrong tool. The TS ecosystem has its own runners (`tsc`, `eslint`, `prettier`, `node --test`), all wired through `check_codebase.sh`. See Removals for the six deleted files.

### Developer Tests and Notes

- Plan reference: `~/.claude/plans/fizzy-cooking-duckling.md` ("Typescript template gate honesty and transferability"). Five of six workstreams landed (WS-PRETTIER, WS-ESLINT, WS-TSCONFIG, WS-PROPAGATOR, WS-CHANGELOG); WS-SELFGATE was prototyped, verified passing, then deleted at user request (see Decisions and Failures above).
- Six-agent `/audit-code-reviewer` pass ran against the change set mid-session (Plan, Test, Style, Docs, Legacy, Comment). Findings adopted in this same entry: e2e script deletion (Decisions auditor pre-empted by user), four-pytest cleanup (Test auditor F-2/F-3/F-4 + user concurrence), TESTS_README.md rewrite (Docs F-1 + Legacy F-1), `OVERLAY_DOTFILE_ALLOWLIST` removal (user-driven simplification after Style auditor noted it as the only remaining hardcoded list in the patch), README `run_all.sh` reference fix (Docs F-3), TS18003 false-claim fix in both shipped files (Plan high finding). pytest after the sweep: 457 passed (down from 476; -19 for the six TS-test deletions).

## 2026-05-23

### Behavior or Interface Changes

- Backported three consumer-side improvements to `templates/typescript/check_codebase.sh` so every typescript consumer benefits on next propagation sync. (1) Added `--no-error-on-unmatched-pattern` to the eslint invocation so fresh consumers without `src/**/*.ts`, `tests/**/*.ts`, or root `*.ts` no longer fail the gate on empty globs (robustness fix; flag is unconditionally safe). (2) Inserted a new step 5 `css:policy` between `format:check` and `test:node` that runs `python3 tools/check_css_content_policy.py`, gated on `[ -f tools/check_css_content_policy.py ]` like the existing `tsconfig.lint.json` gate -- consumers without the script get a SKIP, consumers with it get the check, no template-side stub script required. (3) Added `--import tsx` to the `test:node` step so `.mjs` test files can import `.ts` source modules directly via the tsx runtime loader; added `"tsx": ">=4.22.3"` to `templates/typescript/noexist/package.json` devDependencies (current npm registry version queried via `npm view tsx version`). Inline + header comments explicitly distinguish `tsx` (the npm runtime-loader package by privatenumber, https://www.npmjs.com/package/tsx) from `tsc` (the TypeScript compiler binary shipped inside the `typescript` package) since the two names look alike but are unrelated -- the comment catches the misread before a future reviewer assumes they are interchangeable. Header docstring updated from 5 steps to 6 to match the now-six-step execution flow. Decisions and Failures: initial reviewer rejection of the tsx backport on supply-chain grounds ("tsx is not a real package, potential hack target") was withdrawn after verifying tsx is a real, widely-trusted npm package (~5M+ weekly downloads); the residual supply-chain surface is the same as for the other devDependencies already pinned in `package.json`, and the `>=` floor matches the repo convention from the 2026-05-21 `>=current-latest` pinning sweep.
- Expanded `tests/fix_ascii_compliance.py` replacement table from ~30 entries to ~110, focused on Markdown-friendly substitutions per `docs/MARKDOWN_STYLE.md` ("escape UTF-8 symbols such as &alpha;, &beta;, etc."). Replacement conventions chosen to match the existing `&micro;` precedent: ASCII when a natural ASCII form exists (arrows, math comparators), named HTML entity when one is well-defined (Greek letters, set ops, logic ops), numeric HTML entity for subscripts/superscripts that lack short names. Six user-directed overrides take precedence over the default convention: U+2713 check mark -> "[OK]", U+2717 ballot x -> "[x]", U+2194 left-right arrow -> "<->", U+21C6 leftwards-over-rightwards arrow -> "<~>" (visually distinct from the standard "<->" used for U+2194 and U+21C4 since the glyph itself is a parallel-arrow variant, not a single double-headed arrow), U+03A3 capital Sigma -> "Sum()" (treated as the summation operator -- `&Sigma;` would render as the letter, "Sum()" carries the math semantic that authors mean in 99% of cases). Other additions: extended arrows (U+2191/2193 -> `&uarr;`/`&darr;`; U+21D0/21D2/21D4 -> "<="/"=>"/"<=>"; U+21C4 -> "<->"); Greek capitals A through Omega minus Sigma (24 letters -> `&Alpha;` through `&Omega;`); Greek lowercase alpha through omega (24 letters; U+03BC stays -> `&micro;` per SI-prefix convention); set theory (`&isin;`/`&notin;`/`&ni;`/`&cap;`/`&cup;`/`&sub;`/`&sup;`/`&sube;`/`&supe;`/`&empty;`); logic (`&forall;`/`&exist;`/`&and;`/`&or;`/`&perp;`); extended math (`&equiv;`/`&cong;`/`&prop;`/`&infin;`/`&radic;`/`&sum;`/`&prod;`/`&part;`/`&nabla;`/`&int;`); subscripts 0-9 and superscripts 0-9 (U+00B2 and U+00B3 are ISO-8859-1 but mapped to `&sup2;`/`&sup3;` for visual consistency with their U+2070-series siblings); check/cross variants (U+2705 white heavy check mark, U+2611 ballot box with check, U+274C cross mark, U+2610 ballot box) collapse to the same "[OK]"/"[x]" pair as the bare U+2713/U+2717 codepoints so a Markdown author cannot accidentally introduce two different rendered shapes for the same semantic. Two-token forms "U+2713 Yes" -> "Yes" and "U+2717 No" -> "No" stay first in the pipeline; bare-form replacements only fire on the residue, preserving the prior behavior. Comments now annotate each block with the codepoint family and the rationale for the chosen ASCII vs entity output. File itself remains strict ASCII (verified via `python3 tests/check_ascii_compliance.py -i tests/fix_ascii_compliance.py`, exit 0); the literal target characters are stored as UTF-8 inside Python string literals, which is the same shape the file used pre-expansion for U+2192 etc. (Python source files may contain any Unicode in string literals; only the surrounding code stays ASCII). Decisions: rejected switching the bare-checkmark replacement from "[OK]" to "[x]" mid-pass after user clarified the intent was "[OK]" for U+2713 and "[x]" for U+2717 (initial draft had "[x]"/"[ ]" for symmetry with GitHub task-list rendering; user override preserved). `tests/check_ascii_compliance.py` (the read-only validator) was not changed -- it lints by codepoint range, not by replacement table, so it inherits the expanded coverage automatically.
- Synced `templates/typescript/devel/setup_typescript.sh` with the updated `check_codebase.sh` step list. Added a header block enumerating which npm packages each check step requires (typescript -> tsc; eslint + @eslint/js + typescript-eslint + globals -> eslint; prettier -> prettier; tsx -> node --import tsx) and noting that the CSS content policy step uses system python3, not an npm dep. The header explicitly calls out tsx as the runtime loader and disambiguates it from tsc so a future reader does not assume the two are interchangeable (same disambiguation note now appears in three places: `check_codebase.sh` header, `check_codebase.sh` step 6 inline comment, and `setup_typescript.sh` header). Fixed stale "Setup complete" hint that still advertised the removed `--fast` flag and the removed build step (both deleted from `check_codebase.sh` per the 2026-05-22 self-contained rewrite entry); hint now reads `npm run check - full gate (typecheck, lint, format-check, css-policy, tests)` so it matches what the script actually runs. Install path itself unchanged -- `npm install` already reads every devDependency from package.json, so the new tsx entry installs automatically alongside the existing eslint/prettier/typescript stack with no separate install command needed.

## 2026-05-22

### Additions and New Features

- Added `## Is this a good pytest?` checklist section to `docs/PYTEST_STYLE.md`, inserted between the intro line and the existing `## Test structure` heading. 17 GitHub task-list (`- [ ]`) items covering the brittle-test rules already documented in the **Brittle tests** section: stability (passes next week, independent of today's date / "now"), assertion shape (asserts meaningful output / required behavior / callable behavior / user-visible behavior / object behavior, not collection length / required-key lists / function-name lists / hardcoded defaults / dataclass field assignment), runtime budget (works offline, deterministic timing and fixed seeds, writes only inside `tmp_path`, finishes well under one second), scope (one or two assertions per function, no complex logic in test body, not targeting `_temp.*` or ad-hoc debugging scripts). Trailing line cross-references the `#good-tests` and `#brittle-tests` anchors for examples and rationale so the checklist stays a summary surface and cannot drift from the source-of-truth sections. Positive framing chosen for assertion items ("Asserts meaningful output, not collection length") over negative ("No assert on collection sizes") so each line names the behavior that matters; negative framing kept only where positive becomes vague (`Not a trivial wrapper around a stdlib call`, `Not `_temp.*` or ad-hoc debugging scripts`). Tightly related pairs merged (network + subprocess into "Works offline"; sleeps + randomness into "Uses deterministic timing and fixed seeds") to keep the list scannable at 17 items rather than fragmenting to 19. Existing **Brittle tests** prose unchanged; the new section is an ergonomics surface, not a policy change.
- Added the MERGE bucket to `propagate_style_guides.py` (milestone M-MERGE of the audit plan at `compressed-cooking-pnueli.md`). MERGE is a fifth file-copy bucket (alongside `overwrite_files`, `noexist_files`, `devel_files`, `test_files`) for files where the template owns a fenced region and the consumer owns everything outside. Generalizes the two prior ad-hoc patterns: CLAUDE.md's `@`-line preserve hack and the gitignore managed-block merge. Fence convention chosen per file syntax: shell/Python/YAML/gitignore use `# === TEMPLATE-MANAGED START ===` / `# === TEMPLATE-MANAGED END ===`; JavaScript/TypeScript use `// === TEMPLATE-MANAGED START ===` / `// === TEMPLATE-MANAGED END ===`; markdown uses `<!-- === TEMPLATE-MANAGED START === -->` / `<!-- === TEMPLATE-MANAGED END === -->`. Pure JSON is explicitly not a MERGE candidate (no comment syntax); use NOEXIST + extends-pattern instead. Patch breakdown: (M-A) authored `meta/docs/MERGE_BUCKET_SPEC.md` documenting fence convention, the four outcomes (`created`/`merged`/`unchanged`/`error`), every error case, migration protocol for pre-MERGE consumers, and the precedent it supersedes; (M-B) added `propagate.files.merge_file_safe(source, dest, dry_run, counters)` plus the internal `_find_fences(text)` parser that returns the validated fence region or raises `ValueError` with an actionable message on every malformed state (missing start, missing end, duplicate, wrong order, mixed styles); (M-C) added `MERGE_FILES = frozenset({'CLAUDE.md'})` to `propagate/model.py`, the `'merge_files': []` plan-dict key in `compute_propagation_plan`, the post-walker step 6 routing that moves any `MERGE_FILES` entry out of `overwrite_files`/`noexist_files` (with `assert_not_meta()` still firing so META always wins), and the `apply_file_bucket('merge_files', ...)` dispatcher branch wired into `process_repo` between the noexist and devel buckets; (M-D) migrated CLAUDE.md to the formal MERGE bucket: added markdown fences to template CLAUDE.md, deleted the bespoke `merge_claude_md()` function and its overwrite-branch special case in `propagate_style_guides.py:apply_file_bucket`, removed the now-obsolete `test_overwrite_bucket_claude_md_merge` from `tests/meta/test_propagate_helpers.py`; (M-E) three new test files at `tests/meta/`: `test_merge_bucket_roundtrip.py` (6 cases: created-when-dest-missing, replaces-managed-region, unchanged-when-byte-identical, shell-fence-style, js-fence-style, dry-run-does-not-modify), `test_merge_bucket_error_cases.py` (8 cases: missing end fence, missing start fence, reversed order, duplicate start fence, mixed fence styles, consumer plain shape, template missing fences, source missing -- every case asserts dest unmodified), `test_merge_template_has_fences.py` (parametrized across `MERGE_FILES`, asserts each template source exists and parses to a valid fence pair). One-shot migration burden for consumers: existing CLAUDE.md without fences will surface the documented "consumer not at fenced shape; add fences once, then re-sync" error on next sync; user wraps the `@`-imports in fence markers (one edit) and the propagator round-trips cleanly thereafter. Counter accounting: MERGE reuses the existing `merged_count` and `created_count` counters rather than introducing `merged_managed_count` -- semantics align with CLAUDE.md's prior counter usage so the summary line stays stable. Reconciled four pre-existing `tests/meta/test_propagate_plan_matches_legacy.py` failures: `dist_clean.sh` lives at `devel/dist_clean.sh` (the universal-promotion changelog entry from 2026-05-21 documented the intent but the file remained under `devel/`), so the test expected-set now lists it under `expected_universal_devel` not `expected_universal_overwrite`; also reflected the M0/0B move of `build_github_pages.sh` from `expected_ts_overwrite` to `expected_ts_noexist`. All 475 pytest pass; pyflakes clean across `propagate_style_guides.py`, `propagate/*.py`, every touched test file.

### Behavior or Interface Changes

- Completed M1, M2, M3 milestones of the propagator audit plan (`compressed-cooking-pnueli.md`) and pruned fragile meta tests in the same pass. M1 produced `meta/docs/active_audits/propagate_classification.md` -- per-file OVERWRITE/MERGE/NOEXIST/META verdict + rationale for every file the propagator ships, with a reachability proof that README cannot enter any bucket through any current walker code path. M2 applied the M1 verdicts: added `tests/TESTS_README.md` to `UNIVERSAL_NOEXIST` in `propagate/model.py` (user-locked NOEXIST seed; consumer test-suite README is not template-owned); `git mv templates/typescript/run_web_server.sh templates/typescript/noexist/run_web_server.sh` and `git mv templates/typescript/tsconfig.json templates/typescript/noexist/tsconfig.json` (consumer chooses serve port + project-specific TS compiler options; tsconfig is pure JSON so MERGE is not viable per the M-MERGE spec); added a Classification criterion section to `meta/docs/PROPAGATION_RULES.md` documenting the rule M1 used. Extended step 4 in `propagate/files.py:compute_propagation_plan` to also remove UNIVERSAL_NOEXIST entries from `test_files` (not only `overwrite_files`); the universal tests-walker routes `tests/TESTS_README.md` to `test_files` by default, so the prior step-4-only-touches-overwrite logic would leave the entry in the wrong bucket. M3 updated `tests/meta/test_propagate_plan_matches_legacy.py` expected sets: `tests/TESTS_README.md` added to `expected_universal_noexist` for all four repo-type functions; `run_web_server.sh` and `tsconfig.json` moved from `expected_ts_overwrite` to `expected_ts_noexist`; `expected_universal_test` cleared (it previously held only `tests/TESTS_README.md`, which is now in noexist). Pyflakes clean; 467 pytest pass after the prune (was 499; deleted tests detailed below).
- Pruned five fragile meta tests per `docs/PYTEST_STYLE.md` ("when in doubt, delete -- a missing pytest is cheaper than a fragile one") and user direction to thin the suite. (1) Deleted `tests/meta/test_propagate_classification.py` (the M3/3B subset-assertion test) -- redundant with `tests/meta/test_propagate_plan_matches_legacy.py`, which already pins full-set bucket membership for every reclassified file across all four repo types; the subset test added nothing the full-set test did not already catch. (2) Deleted `tests/meta/test_readme_never_propagates.py` -- subsumed by `tests/meta/test_no_meta_leaks.py`, which already iterates every plan entry across every repo type and asserts none match `META_FILES` (README.md is in META_FILES). (3) Deleted `tests/meta/test_typed_walker_meta_guard.py` -- staged synthetic `templates/<fake-type>/README.md` and `templates/<fake-type>/noexist/README.md` via `shutil.copytree` of the entire template repo; the fixture failure mode requires a future contributor to land a template file the M0/0A walker filter already blocks AND escape the `test_no_meta_leaks.py` parametrized check -- redundant coverage at the cost of a slow whole-repo copy. (4) Deleted `tests/meta/test_devel_can_be_noexist.py` -- pinned a routing capability (ROUTING_OVERRIDES `bucket: noexist` on a synthetic `devel/fake_setup.sh`) that no real devel file uses today; the dispatcher logic is already exercised by `pip_requirements.txt` which uses `bucket: noexist`. (5) Deleted `tests/meta/test_merge_template_has_fences.py` -- called the private `propagate.files._find_fences` directly (Test auditor HIGH finding); with one entry in MERGE_FILES (`CLAUDE.md`) and round-trip coverage via `test_merge_bucket_roundtrip.py`, the configuration-side test added little. Kept tests verify real behavior: `test_no_meta_leaks.py` pins the META-never-ships invariant; `test_merge_bucket_roundtrip.py` + `test_merge_bucket_error_cases.py` verify `merge_file_safe` round-trips and error handling per PYTEST_STYLE good patterns ("Round-trip invariants", "Error detection"); `test_propagate_plan_matches_legacy.py` is the canonical full-set regression lock.

### Fixes and Maintenance

- Second audit-code-reviewer pass (run after M1/M2/M3 + pytest prune landed). Six independent reviewers (Plan, Test, Style, Docs, Legacy, Comment) returned findings; applied mechanical fixes in this commit and documented design-judgment items for user decision. Applied: (1) fixed two remaining inert monkeypatches at `tests/meta/test_propagate_helpers.py` lines 633-639 (`test_devel_bucket_no_change_check`) and 696-702 (`test_test_bucket_auto_discovered_files_handled`) -- both targeted `propagate.model.source_path_for_bucket` (the raising variant) but `apply_file_bucket` calls `propagate.model.find_source_for_bucket` (the None-returning variant); same fix as the noexist branches in the prior pass; mocks now intercept the actual call sites; (2) removed the stale "Empty by default" comment on `MERGE_FILES` in `propagate/model.py` (frozenset is no longer empty -- holds CLAUDE.md); (3) stripped task-milestone prefixes from three test module docstrings (`tests/meta/test_no_meta_leaks.py` "M0/0E:", `tests/meta/test_merge_bucket_roundtrip.py` "M-MERGE/M-E:", `tests/meta/test_merge_bucket_error_cases.py` "M-MERGE/M-E:") per `docs/REPO_STYLE.md` "Don't reference the current task, fix, or callers... those belong in the PR description and rot as the codebase evolves"; (4) replaced the ghost `tests/meta/test_merge_template_has_fences.py` reference in `meta/docs/MERGE_BUCKET_SPEC.md:61` (test was deleted in the prior pytest prune; spec error case 6 is now covered by `test_merge_bucket_error_cases.py::test_error_template_missing_fences` at sync time, not plan-build time -- the reword reflects this); (5) replaced rejected `merged_managed` counter name in `MERGE_BUCKET_SPEC.md:83` with the actual implementation (`merged_count` and `created_count`, reused from the pre-M-MERGE CLAUDE.md overwrite-branch merge); (6) rewrote `meta/docs/active_audits/propagate_classification.md` "Reclassification actions for M2" section from imperative (future) tense to past tense ("applied") since all four 2A/2B/2C/2D actions are landed; (7) removed the deleted `test_readme_never_propagates.py` citation from the same audit doc's "Reachability conclusion" sentence; (8) rewrote the audit doc's "Devel-NOEXIST" open follow-up bullet to remove the deleted `test_devel_can_be_noexist.py` citation and to call out the consumer-path-construction gap explicitly so a future contributor knows to re-pin it; (9) added `'merge_files'` to `PATH_BUCKETS` in `tests/meta/test_no_meta_leaks.py:18` -- the META-leak parametrized test was silently skipping the new MERGE bucket (the runtime `assert_not_meta` guard still catches META at plan-build time, but the test-level coverage gap is now closed). Reviewer findings deferred for user decision (not applied): HIGH `source_path_for_bucket` is dead production code -- only two test files (`test_propagate_spec_resolves_to_source.py`, `test_propagate_plan_precedence.py`) still call it; deletion would require migrating both callers to `find_source_for_bucket` plus a `None`-return adjustment (design call: keep raising variant for test-side strict checks, or unify on the live variant); MEDIUM `add_group_write` silent OSError (reviewed twice now, both passes -- conforms to PYTHON_STYLE per the prior pass rationale); MEDIUM `try/except ValueError` around `_find_fences` in `merge_file_safe` (style call -- 2-line rule conforming but Style auditor prefers return-value contract); MEDIUM lambda calling `format_path_pair` in the devel bucket (style consistency with named-inner-function pattern in overwrite/test buckets); LOW `copy_file_safe` signature `message: str = None` should be `str | None = None`; HIGH from Docs auditor -- `--bootstrap` interaction with META filtering needs CLI help or `PROPAGATION_RULES.md` note (the Open decision flagged in the plan); MEDIUM Docs auditor -- consumer-facing notice for the three typescript files that just moved from OVERWRITE to NOEXIST (release-note placement is a user design call -- `docs/NEWS.md` does not exist in this template); LOW empty `templates/rust/noexist/` directory observation was a Plan auditor false positive (verified `ls templates/rust/` -- only `gitignore.rust` present; the `git rm` of the README cleaned the directory automatically). Test auditor's HIGH on `test_error_consumer_plain_shape_no_fences` was a misreading: the test asserts `'error'` for plain consumer + fenced template, which matches `MERGE_BUCKET_SPEC.md:41` row 3 and section 7 "Plain consumer, fenced template" -- the spec evolved during M-A; current implementation and test agree. 482 pytest pass; pyflakes clean across edited files.
- Cleared three of the audit-code-reviewer HIGH findings on the M-MERGE diff that were obvious follow-ons. (1) `tests/meta/test_propagate_helpers.py` -- four `monkeypatch.setattr(propagate.model, 'source_path_for_bucket', ...)` calls were inert: production `apply_file_bucket` exclusively calls `propagate.model.find_source_for_bucket` (the None-returning variant), so the patches never intercepted anything and tests passed for the wrong reason (the real source files happened to exist in the fixture trees). Renamed the patch target to `find_source_for_bucket` across all four mocks and changed the fallback from `raise FileNotFoundError()` to `return None` to match the variant's contract. Updated the surrounding `# Mock source_path_for_bucket` comments. (2) `meta/docs/PROPAGATION_RULES.md` -- added the MERGE bucket as a row in the Folder convention table ("Fenced merge (template owns a region, consumer owns the rest)" -> "template root + add to MERGE_FILES" -> "every repo, fenced merge"); inserted MERGE_FILES as step 2 in the Precedence section (between META_FILES and ROUTING_OVERRIDES, documenting that post-walker step 6 routes MERGE entries out of overwrite/noexist into merge_files); added the MERGE_FILES manifest bullet to the "Exceptions in the manifest" section (with a link to `MERGE_BUCKET_SPEC.md`); fixed the stale "CLAUDE.md is allowlist-only, so it overwrites" claim to reflect the M-D migration ("allowlisted and routed via MERGE_FILES (fenced merge)"); bumped the "four short manifests" count to five. (3) Style auditor's CRITICAL on the silent except in `add_group_write` (`propagate/files.py:669-672`) was reviewed and not fixed: the construct catches a narrow `OSError` (not broad `Exception`), the body is 2 lines, and the comment documents the intent (best-effort group-write for broken symlinks / vanished files) -- conforms to `docs/PYTHON_STYLE.md` "Use try/except rarely, and if needed use for at most two lines". 40 `test_propagate_helpers.py` cases pass; 157 `tests/meta/` cases pass; pyflakes clean.
- Applied audit-code-reviewer low-risk findings on the M-MERGE diff. Fixed stale comment in `propagate/model.py:45` that claimed `CLAUDE.md` "overwrites" (it now MERGE-routes via `MERGE_FILES`). Added missing `#====` visual separator before `source_path_for_bucket` in `propagate/model.py` to match the file's other top-level defs. Replaced the tombstone "META guard: parity with universal walker above" in `propagate/files.py` with a standalone WHY (typed overlays must filter template-internal files so a stray `templates/<type>/<META-name>` cannot ship). Rewrote the confusing "which we replicate to preserve newline behavior on join" comment in `_find_fences` to state the WHY directly: `split('\n')` round-trips a trailing newline byte-for-byte via `'\n'.join()`, where `splitlines()` would silently drop it. Updated `meta/docs/MERGE_BUCKET_SPEC.md` to past-tense the now-historical `@`-line preserve hack (`CLAUDE.md` migrated to MERGE during M-D; the spec was still describing the hack in present tense as a live mechanism). Reviewer findings deferred for design judgment (not applied): two BLOCKER `copytree` slow-pytest cases in `test_typed_walker_meta_guard.py` and `test_devel_can_be_noexist.py`; CRITICAL silent `except` in `add_group_write`; HIGH `_find_fences` private-API call in `test_merge_template_has_fences.py`; HIGH `PROPAGATION_RULES.md` missing MERGE row + precedence step + manifest entry; HIGH style nits (bare `tuple` annotation, `message: str = None`, lambda in devel bucket); HIGH inert `monkeypatch.setattr(propagate.model, 'source_path_for_bucket', ...)` in `tests/meta/test_propagate_helpers.py` (production calls `find_source_for_bucket`, so the mocks never intercept anything); MEDIUM `merge_conftest` parallel ad-hoc merge path outside the MERGE bucket framework; MEDIUM `merged_count` vs plan-literal `merged_managed` naming. Pyflakes clean across edited files.
- Hardened `propagate_style_guides.py` against silent META leaks across multiple walker code paths (M0 milestone of the audit plan at `compressed-cooking-pnueli.md`). Six concrete patches: (1) typed-overlay walker (`propagate/files.py:848-883`) now applies the same `META_FILES` + `META_DIRS` check that the universal walker uses, closing a gap where a stray `templates/<type>/<META-name>` would silently ship as OVERWRITE; (2) `git mv templates/typescript/build_github_pages.sh templates/typescript/noexist/build_github_pages.sh` so consumer deploy customizations are no longer clobbered on sync (user-directed reclassification); (3) consolidated `BLOCKED_DOCS_FILES` (function-local frozenset in `propagate/files.py` scoped to `docs/`-prefixed paths) into `META_FILES` in `propagate/model.py` -- one "never ships" concept with one mechanism; `docs/CHANGELOG.md` now lives in `META_FILES` directly and the function-local set is deleted; (4) added `propagate.files.assert_not_meta(file_rel)` helper that raises `RuntimeError` if a path matches `META_FILES` by basename / rel-path or traverses any `META_DIRS` component, wired at every `plan[bucket].append` site in `compute_propagation_plan` and at the top of `apply_file_bucket` -- defense in depth that catches both plan-construction leaks (a future walker branch that forgets the META filter) and plan-injection leaks (a future feature that reads a plan from disk or accepts user-supplied paths); (5) `git rm templates/rust/noexist/README.md` -- README must never ship, even via noexist semantics, because the generic rust stub ("Rust toolchain follow-up plan; no canonical files yet.") would overwrite when a consumer README was missing or recently truncated; the typed-noexist subwalker (`propagate/files.py:863-867`) now also filters `META_FILES` on the *consumer_path* (the path after stripping the `noexist/` prefix) so a future contributor cannot re-introduce a typed README stub through this code path either; (6) Six new pytest cases under `tests/meta/`: `test_no_meta_leaks.py` (parametrized across every repo_type, asserts no plan entry's basename / rel-path / path-component matches META_FILES or META_DIRS, plus three small unit tests for the helper itself); `test_readme_never_propagates.py` (parametrized across every repo_type, asserts README absent from every bucket); `test_typed_walker_meta_guard.py` (stages a synthetic `templates/<fake-type>/README.md` and `templates/<fake-type>/noexist/README.md` in tmp_path clones and asserts both the typed-overlay walker and typed-noexist subwalker skip the stray fixtures); `test_devel_can_be_noexist.py` (pins the future-proofing capability that `ROUTING_OVERRIDES` `bucket: noexist` works for a `devel/<x>.sh` file -- no devel file uses NOEXIST today, but the mechanism is pinned with a fixture test that monkeypatches a synthetic override and asserts the entry lands in `noexist_files` with the full `devel/<x>.sh` consumer-side path preserved, not in `devel_files`). All gates green: 16 new tests pass; 4 pre-existing failures in `tests/meta/test_propagate_plan_matches_legacy.py` unchanged (dist_clean.sh absence is an upstream issue out of scope for this milestone); `pyflakes propagate_style_guides.py propagate/*.py tests/meta/test_*.py` clean.

### Behavior or Interface Changes

- Made `templates/typescript/check_codebase.sh` self-contained: inlined the five remaining `npm run --silent <name>` invocations (typecheck, typecheck:lint, lint, format:check, test:node) as direct tool calls (`npx tsc -p tsconfig.json`, `npx tsc -p tsconfig.lint.json`, `npx eslint --max-warnings 0 ...`, `npx prettier --check ...`, `node --test 'tests/test_*.mjs'`). Deleted the `require_script` probe function and every callsite (no more `node -e "process.exit(((require('./package.json').scripts)||{})[name] ? 0 : 1)"` preflight per step). Rewrote `step_run` to accept the command as positional args (`step_run <name> <cmd...>`); the function shifts the name off and runs `"$@"` directly with no `eval`. Removed the build step entirely from `check_codebase.sh` -- build belongs in `build_github_pages.sh` -- and removed the `--fast` flag plus its `FAST=` handling along with it (argparse minimization per `docs/PYTHON_STYLE.md`: the flag existed only to skip the now-deleted build step). Removed the `HAS_STYLE_CSS` capture and the post-build artifact existence checks (`dist/index.html`, `dist/main.js`, `dist/style.css`) since the build step is gone. Header comment rewritten to drop the "`package.json` remains the single source of truth" claim and the build-step description; new header documents that each step runs its tool directly and that build/Playwright are explicitly out of scope.
- Promoted `dist_clean.sh` from `templates/typescript/` to repo root and added it to `ROOT_PROPAGATE_ALLOWLIST` in `propagate/model.py` so it now ships universally to python, typescript, rust, and other repos via `overwrite_files`. Used `git mv templates/typescript/dist_clean.sh dist_clean.sh` so history is preserved. The script's content already mixed TS-specific patterns (`dist/`, `node_modules/`, `*.tsbuildinfo`, Playwright outputs) with universal patterns (`**/__pycache__`, `**/.pytest_cache`, `**/.mypy_cache`, `**/.ruff_cache`); the typed-overlay home was the wrong file location for that mix. Header rewritten to declare universal scope and document per-language reinstall hints (`npm install` for ts, `pip install -r pip_requirements.txt -r pip_requirements-dev.txt` for python, `cargo build` for rust). Added Rust build output (`target/`) and generic build dirs (`build/`, `out/`) to the sweep so the script does something useful in a Rust or Python repo too; existence-checked deletion via `delete_if_exists` keeps the output honest (paths that do not exist are not listed). Updated `tests/meta/test_propagate_plan_matches_legacy.py`: moved `'dist_clean.sh'` out of the typescript-specific `expected_ts_overwrite` set and into `expected_universal_overwrite` for all four repo-type tests (python, typescript, rust, other). Verified via dry-run plan: `'dist_clean.sh' in plan['overwrite_files']` returns `True` for every repo type. The `templates/typescript/noexist/package.json` `"clean": "./dist_clean.sh"` alias still resolves because the script lands at consumer root, the same path it landed at before. `templates/typescript/docs/TYPESCRIPT_STYLE.md` link `[dist_clean.sh](../dist_clean.sh)` (from `docs/`) still resolves correctly to the new root location. 445 pytest pass.
- Expanded `templates/typescript/dist_clean.sh` from a narrow `rm -rf dist _site _bundle.js *.tsbuildinfo .eslintcache` sweep to a full clean. New sweep removes: build outputs (`dist/`, `dist-single/`, `_site/`, `_bundle.js`), esbuild meta (`meta.json`, `stats.html`), TypeScript incremental info (`**/*.tsbuildinfo` at any depth), dependency installs and lockfile (`node_modules/`, `package-lock.json`), JS/TS tool caches (`.cache`, `.eslintcache`, `.prettiercache`, `.nyc_output`), test outputs (`test-results/`, `playwright-report/`, `blob-report/`, `coverage/` -- all sourced from `templates/typescript/gitignore.typescript`), and Python bytecode/tool caches at any depth (`**/__pycache__`, `**/.pytest_cache`, `**/.mypy_cache`, `**/.ruff_cache`). Enabled `shopt -s globstar nullglob` so the `**/` patterns match nested instances and a missing top-level match silently no-ops. Lockfile and `node_modules/` are part of the sweep so the next `npm install` produces a reproducible install from `package.json` floors rather than reusing a stale resolved tree; the header comment notes that `npm install` must run again before `npm run check`/`build`/`serve` will work. Legacy `_bundle.js` sweep kept for one more release per the existing comment. Output now reports only paths that were actually deleted: a `delete_if_exists` helper checks `[ -e ]` / `[ -L ]` before each `rm -rf`, appends successfully removed paths to a `DELETED` array, and prints `Cleaned N path(s):` followed by one path per indented line. When nothing is on disk the script prints `Nothing to clean.` instead of the previous misleading static "Cleaned ..." list that named items the script had not actually removed.
- Trimmed `templates/typescript/noexist/package.json` `scripts` block from 16 entries to 8 by removing the redundant single-tool aliases (`typecheck`, `typecheck:lint`, `lint`, `format`, `format:check`, `format:write`, `test:node`). Kept the eight canonical wrappers that point at shell scripts or compound tools: `setup` (devel/setup_typescript.sh), `setup:playwright` (devel/setup_playwright.sh), `build` (build_github_pages.sh), `serve` (run_web_server.sh), `check` (check_codebase.sh), `clean` (dist_clean.sh), `test:playwright` (playwright test), `pdf` (tools/html_to_pdf.mjs). Design: `templates/typescript/check_codebase.sh` lived in the propagator's overwrite bucket (ships every run) while `templates/typescript/noexist/package.json` lived in the noexist bucket (ships once at bootstrap, never overwritten). Any new script added to `check_codebase.sh` would fail the `require_script` preflight on existing consumers whose `package.json` was seeded before the change. Collapsing the contract surface to a single self-contained script removes the cross-bucket coupling at the source; the rejected alternative was a propagator-time managed-block merge of required scripts into consumer `package.json` (more machinery, no clear win once the shell script can call the tools itself). Verified the other three `templates/typescript/*.sh` scripts (`build_github_pages.sh`, `dist_clean.sh`, `run_web_server.sh`) already have no `npm run` references and were not edited.

## 2026-05-21

### Behavior or Interface Changes

- Replaced the pre-editor diff dump in `devel/commit_changelog.py` with a rendered seed-message preview. The seed message (what actually gets committed) now prints before the "Add to the commit message?" prompt via a bold header ("Seed commit message:"), the seed text rendered with `markup=False` so any literal `[brackets]` in entry titles do not get parsed as rich markup, and a trailing rule for visual separation. The diff-to-stderr preview path is entirely removed; `get_diff` and `get_cached_diff` remain as the empty-tree guard for short-circuiting on clean trees. Design: this fix shows the exact artifact headed for `git commit` instead of hiding it under a diff dump. Removed the now-dead `print_diff_to_stderr` function. Pyflakes clean; 439 pytest pass.
- Dropped the `.template` suffix from the TypeScript bootstrap package.json source. `git mv templates/typescript/noexist/package.json.template templates/typescript/noexist/package.json` so the noexist bucket ships `package.json` directly to consumer root (no transient `.template` file ever lands in a consumer repo). Renamed `reset_repo.substitute_and_mv_typescript_template()` -> `substitute_typescript_package_json()`: operates in-place on `package.json`, guards on the literal `__REPO_NAME__` marker (skips substitution when the consumer already customized the file), drops the `git mv` step entirely. Reordered the call site to run AFTER `run_propagate` since the noexist copy is what places the file at consumer root. Updated two meta tests (`test_propagate_folder_convention.py`, `test_propagate_plan_matches_legacy.py`) to expect `package.json` not `package.json.template`. Rationale: user invariant -- no file ends up in a consumer repo with a `.template` suffix, ever. Pyflakes clean; 16 meta tests pass; `git ls-files | grep "\.template"` returns nothing.
- Moved `tools/detect_repo_type.py` exclusion from per-file `META_FILES` to dir-level `META_DIRS` (`'tools'`) and `SKIP_WALK_DIRS`. Cleaner principle: directory convention for whole-dir exclusions, explicit per-file list only when root-level meaning is ambiguous. `reset_repo.py` already `git_rm_recursive("tools/")` at consumer bootstrap so behavior unchanged. Renamed `tests/meta/test_propagate_folder_convention.py::test_meta_file_excluded_path_form_tools` to `test_meta_dir_excludes_tools_nested` to reflect the dir-form coverage. 397 pytest pass; pyflakes clean.
- Rewrote `templates/typescript/check_codebase.sh` as a CLI-friendly orchestrator over `npm run`. Added flags `--fast` (skip Playwright and build), `--skip-playwright` (skip Playwright only), and `-h`/`--help`. Preflight verifies `node` and `npm` on PATH, prints `node $(node --version), npm $(npm --version)`, requires `package.json` and `node_modules`, and warns (does not fail) when `package-lock.json` is missing. Each step probes that the named npm script exists in `package.json` via a small `node -e` snippet so there is no `jq` dependency. Step outcomes are recorded into parallel indexed arrays (bash 3.2 compatible) as PASS, FAIL, or SKIP, and a per-run `Summary:` block plus `N of M checks failed.` line prints on exit; the summary is suppressed for `--help`, `-h`, unknown-flag exits, and preflight failures so non-run paths stay quiet. The post-build artifact check asserts `dist/index.html` and `dist/main.js`, and only asserts `dist/style.css` when `src/style.css` existed at the start of the run. The source-of-truth contract is preserved: every step still runs via `npm run --silent <name>`, with `package.json` defining the checks and the shell script only orchestrating them. Smoke tested from the template root: `--help` and `-h` print usage and exit 0 with no summary; `--bogus` writes the error plus usage to stderr and exits 2 with no summary; bare invocation fails preflight on the missing `package.json` and exits 1 with no summary.
- Improved the TypeScript template check runner summary output.
- Removed the Playwright step from the TypeScript template `check_codebase.sh`; Playwright walkthroughs are invoked manually via `npm run test:playwright`. The `--skip-playwright` flag is gone and `--fast` now skips only the build step.
- Expanded `devel/changelog_lib.py` from a parser/serializer into the canonical shared module for changelog-oriented developer scripts. Lifted the git helper trio (`run_git`, `get_git_root`, `ensure_in_git_repo`) out of `devel/commit_changelog.py`, `devel/rotate_changelog.py`, and `devel/query_changelog.py` (eliminating the three-way duplication flagged by the prior Legacy auditor) into a new `# Git helpers` section in the library. Also lifted the console + prompt helpers (`build_choice_prompt`, `confirm`, `print_warning`, `print_error`) plus module-level `CONSOLE` / `ERR_CONSOLE` (both built with `highlight=False` to suppress the "pink lemonade" auto-coloring on numbers and paths) out of `commit_changelog.py` and `rotate_changelog.py` into a new `# Console and prompt helpers` section. Caller scripts now reference `changelog_lib.X` everywhere; no caller still owns local copies. `devel/query_changelog.py` keeps its own local `ERR_CONSOLE` since it predates the lift; updated it to `highlight=False` to match. `devel/submit_to_pypi.py` keeps its own `console`/`error_console` (now renamed to ALL_CAPS `CONSOLE` / `ERROR_CONSOLE` per repo style) and its own `print_*` helpers, since they pass slightly different rich args (`highlight=False, markup=False` on every print) -- separation kept intentionally per user decision. The library docstring was rewritten ("Shared helpers for changelog-oriented developer scripts") and the file reorganized under three top-level banners: `# Changelog parsing and serialization`, `# Git helpers`, `# Console and prompt helpers`. This consolidation overrides the original plan's "deferred to a separate `devel/git_helpers.py` follow-on" decision -- user chose the in-library home to keep the number of imported modules low. Also unified rich print arguments across `devel/`: every `print_*` helper across `changelog_lib.py` and `submit_to_pypi.py` now passes `highlight=False, markup=False`. Pyflakes clean across all 5 devel files; 136 tests/meta pass; rotate `--dry-run` and query `--count` smokes green.
- Rewired `devel/commit_changelog.py` to build its seed commit message from parsed changelog entries instead of raw `git diff` output. New helpers: `get_last_changelog_commit_date()` (reads `git log -1 --format=%cI -- docs/CHANGELOG.md` and takes the leading 10 chars as the cutoff `YYYY-MM-DD`; `None` for first-time use), `select_new_entries(cutoff_date)` (calls `changelog_lib.parse_file`, deduplicates parse warnings, returns `(entries, warnings)` where entries are filtered `entry.date >= cutoff_date`), and deterministic text cleaners `strip_markdown_links`, `strip_markdown_bold`, `collapse_whitespace`, `truncate_text`, and `clean_entry_text(text, max_length=72)`. New `make_seed_message_from_entries(entries)` builds the seed from `Entry` records: single-entry subject uses the cleaned title; multi-entry subject is `f"{first_title} (+{N-1} more)"` re-truncated to 72; body emits `- ` title lines (length 100) with two-space-indented continuation when `entry.body` is non-empty; multi-day entry lists emit `## YYYY-MM-DD` group headings; total body output capped at 25 emitted lines. `main()` first guards on an empty `git diff` + `git diff --cached` (prevents phantom commits on clean trees when today's entries match the cutoff), then runs the parse-based selection, prints deduplicated parse warnings once via `print_warning`, and emits a cutoff-aware "Nothing to commit" message when no entries pass the filter. The diff-to-stderr preview before the editor opens stays as-is. Removed the now-dead `extract_added_lines`, `build_message`, and `make_seed_message` helpers. Design: date-window selection intentionally **excludes edits to older changelog entries** (legacy cleanup, link fixes, typo passes) because those should never seed today's commit subject; same-day duplicates are acceptable v1 noise that the user can prune in the editor buffer. Explicit non-goal: no LLM summarization (brittle external dependency, deterministic cleaners are sufficient). Tests added at `tests/meta/test_commit_changelog.py` (23 cases: cleaner round-trips, single/multi-entry subjects, multi-day grouping, body line cap, link/bold strip in subject; pure-function coverage with fabricated `Entry` records, no git, no filesystem). Smoke A passes (clean working tree -> "No changes... Nothing to commit."); interactive smokes B-F (single bullet, multi-day, malformed `## 2026-13-99`, same-day second commit, old-entry edit) require user-driven verification per the plan. Pyflakes clean.
- Refactored the changelog devel tools to share a single parser/serializer. Extracted `devel/changelog_lib.py` (data model, regex constants, file I/O, parser, day-block-to-entry splitter) and rewired `devel/rotate_changelog.py` + `devel/query_changelog.py` as thin shells that delegate parsing and serialization to the library. The library exposes `DayBlock` and `Entry` `@dataclasses.dataclass` records (not dicts), `parse_day_blocks(text, source, duplicate_policy)` with `'warn'` / `'raise'` / `'keep'` policies, `split_day_block(block, strict)`, `parse_file`, `read_changelog`, `write_changelog` (preserves day-block `raw_text` verbatim; normalizes final newline to exactly one), `newest_date`, `find_duplicate_dates`, and `CANONICAL_CATEGORIES`. Rotation now preflights duplicate `## YYYY-MM-DD` headings via `parse_day_blocks(duplicate_policy='keep')` + `find_duplicate_dates()` and refuses to write when duplicates exist, since whole-day serialization could otherwise drop or misarchive data. Calendrically invalid date headings (e.g. `## 2026-13-99`) warn + skip the entire block up to the next valid heading. Library does NOT own CLI, git, prompts, console styling, or query-side filtering; those stay in the scripts. Updated `tests/test_query_changelog.py` to import parse helpers from `changelog_lib` and switch dict subscripts to dataclass attribute access. Added `tests/test_changelog_lib.py` (17 tests: round-trip, tolerance, duplicate policies, final-newline normalization, helpers) and `tests/test_rotate_changelog.py` (7 tests: `split_active_archive` and `find_boundary_conflict`). Gates: 462 pytest pass; pyflakes clean across the three devel files; `query_changelog` and `rotate_changelog` CLI behavior unchanged.

### Additions and New Features

- Added `meta/tools/sync_typescript_package_pins.py`: walks the current working directory recursively for every `package.json` and rewrites its `devDependencies` pins to `>=<current-latest>` per `npm view <pkg> version`. No filtering by signature, repo type, name, or path -- discovery is intentionally promiscuous so a single run from `~/nsh` covers every consumer repo, every skill template, and every loosely-related npm project. Walk pruning skips noisy trees only (`node_modules`, `.git`, `dist`, `build`, `__pycache__`, virtualenvs, dotdirs). Pin shape is uniformly `>={latest}` to avoid the npm 0.x caret quirk (caret on 0.x treats every minor as breaking, which would forbid the current 0.28.x esbuild line under `^0.25.0`). Full patch version is preserved (e.g. `>=8.59.4`, not `>=8.59.0`) so CVE-floor precision survives runs (Playwright's `curl -k` fix landed in 1.55.1, not 1.55.0; rounding patch to zero would risk floors below known security fixes). CLI surface: one flag, `--apply`; default mode prints per-target diff and writes nothing. Consumer extras the union does not declare are still listed in the npm query (collected from every target's devDependencies, not just from a canonical template) and bumped alongside the rest. Lives under `meta/tools/` so it is excluded from propagation to consumer repos (`meta/` is in `META_DIRS` per `docs/REPO_STYLE.md`). Sample invocation: `cd ~/nsh && starter-repo-template/meta/tools/sync_typescript_package_pins.py [--apply]`. Smoke run from `~/nsh` discovered 18 `package.json` files, queried 42 unique packages, and rendered a clean dry-run diff across consumer TS repos, the skill template, and unrelated npm projects (PG_v2.17, webwork-pg-renderer, etc.). Pyflakes clean. Script name retains `typescript_` prefix even though it no longer filters by repo type; rename deferred until the user requests it.
- Added VERSION file freshness check to `devel/commit_changelog.py`. New helper `read_version_file()` reads the VERSION file relative to the repo root and returns its stripped contents; `current_calver_month()` returns the current CalVer month in YY.MM format (e.g. "26.05"); `check_version_freshness()` compares the first two dotted segments of the VERSION file against the current month and prompts the user if they don't match using `changelog_lib.confirm()`. The prompt is called in `main()` immediately after the merge-conflict and untracked-file preflights and before the `get_diff` call. On user decline, the script prints "Aborted." and returns. This check catches drift when the VERSION file has not been updated to match today's calendar month (per CalVer in `docs/REPO_STYLE.md`).
- Added `## Hardcoding principles` section to `meta/docs/PROPAGATION_RULES.md` documenting the three-tier categorization (good/expected/bad hardcoding) and the cleaner principle: directory convention when possible, explicit list when root-level meaning is ambiguous, override table only for true routing exceptions. Updated `## Exceptions in the manifest` to document the `ROOT_PROPAGATE_ALLOWLIST` vs `UNIVERSAL_NOEXIST` composition (allowlist decides IF a root file ships; noexist refines HOW). Mirrored the composition rule as in-source comments above both sets in `propagate/model.py`.
- Added two changelog devel tools at `devel/rotate_changelog.py` and `devel/query_changelog.py`. `rotate_changelog.py` automates the rotation rule in `docs/REPO_STYLE.md`: keeps the two most recent `## YYYY-MM-DD` day blocks in the active file, archives older blocks into `docs/CHANGELOG-YYYY-MM[a-z].md` (named after the most recent archived month, next unused letter suffix), and refuses to silently drop a boundary date that already exists in an archive without an explicit `--yes`. Flags: `-n/--dry-run`, `-f/--force`, `-t/--threshold N` (default 1000 lines), `-y/--yes`. `query_changelog.py` makes the CHANGELOG corpus queryable for release archaeology: filters by inclusive `--from`/`--to` date range or relative `--since N` days; by category (canonical heading or short alias `add`/`behavior`/`fix`/`remove`/`decision`/`test`); by repeatable plain-substring `-k/--keyword` (AND by default, `--any-keyword` for OR, `--case-sensitive` toggle); by `--source` file. Default scope is the active changelog; `--archives` (alias `--all`) expands to every `docs/CHANGELOG-YYYY-MM[a-z].md`. Output formats: `text` (default), `json`, `csv`; `--count` prints only the matching count. Error tolerance: malformed CHANGELOG content (bad date headings, bullets before any `### Category`, duplicate dates inside one file, non-canonical headings) warns to stderr and continues parsing; CLI input errors and missing-active-changelog hard-raise. Both scripts inlined a small `parse_day_blocks` helper with a sync comment pointing at the sibling script in v1; the shared `devel/changelog_lib.py` was extracted on the same day in v2 (see Behavior or Interface Changes above). Tests added at `tests/test_query_changelog.py` (19 cases, run in 0.09s); rotation is verified by smoke runs, not by pytest. Gates: 408 pytest pass; pyflakes clean on both scripts.

### Fixes and Maintenance

- Replaced every `"*"` devDependency pin in `templates/typescript/noexist/package.json` with `">=<current-latest>"`. Resulting floors (queried via `npm view <pkg> version`): `esbuild >=0.28.0`, `typescript >=6.0.3`, `eslint >=10.4.0`, `@eslint/js >=10.0.1`, `typescript-eslint >=8.59.4`, `globals >=17.6.0`, `prettier >=3.8.3`, `playwright >=1.60.0`, `@playwright/test >=1.60.0`. Rationale: bare `"*"` silences Dependabot and offers no security floor; we do not support versions older than today, so `>=current-latest` is the right shape. `>=` (not `^`) is used uniformly because npm's caret semantics treat each 0.x minor as breaking, which would forbid the current 0.28.x esbuild line under `^0.25.0` and force a split convention across packages. The esbuild and playwright floors also clear the two CVEs flagged by Dependabot (esbuild dev-server CORS, fixed in 0.25.0; playwright macOS browser-reinstall `curl -k`, fixed in 1.56.0); both CVEs were low practical risk in this repo (template uses esbuild for `build` not `--serve`; the vulnerable playwright scripts are manual macOS browser reinstallers, not the default install flow).
- Fixed a same-day re-inclusion bug in `devel/commit_changelog.py`. The previous selection filter (`entry.date >= cutoff_date`, where `cutoff_date` was the calendar day of the last commit touching `docs/CHANGELOG.md`) returned every bullet under today's `## YYYY-MM-DD` heading whenever the user committed in the morning and then added more bullets under the same heading later the same day. The seed commit message would balloon with `(+N more)` and re-list already-shipped work; today's HEAD `(+15 more)` commit was an instance of this bug. Replaced the date-window filter with a SHA-anchored set-difference: `get_last_changelog_commit_sha()` returns the full SHA of the last changelog-touching commit, `get_changelog_text_at(sha)` fetches the prior file contents via `git show <sha>:docs/CHANGELOG.md`, and a new pure helper `compute_new_entries(current, prior)` keeps only entries whose `(date, title)` identity key is absent from the prior set (preserves current-file order). Identity key is `(date, title)`; a rephrased title is treated as a new entry by design (user prunes in the editor buffer), since fuzzy matching would risk silently dropping legitimate edits. Added `changelog_lib.parse_text(text, source, ...)` as a sibling entry point to `parse_file` so the prior-commit string parses without a temp file; `parse_file` is now a thin wrapper. Added 6 pytest cases at `tests/meta/test_commit_changelog.py` covering empty-prior, same-day second commit (the bug repro), no-change, title rephrase, order preservation, and a `parse_text` round-trip that drives `compute_new_entries` end-to-end through the in-memory parse entry point. The "No new entries..." message now anchors on the short SHA (`No new entries since commit a1b2c3d4.`) instead of the misleading cutoff date. Post-audit follow-ups in the same patch: updated the stale `devel/commit_changelog.py` description in `docs/REPO_STYLE.md` (it still said "dated at or after the last commit" -- now reflects the SHA-anchored set-difference); added `Raises:` clauses to `parse_text` and `parse_file` docstrings documenting the `ValueError` passthrough from `parse_day_blocks` under `duplicate_policy='raise'`. Also dropped the redundant `- title` body bullet from single-entry seeds in `make_seed_message_from_entries`: the subject already states the title, so repeating it as a bullet was pure noise. Single-entry seeds with a body now render the body as a plain paragraph after the blank line (no `- ` prefix, no two-space indent -- that shape is reserved for multi-entry lists). Multi-entry seeds keep the bulleted list shape so each entry stays individually scannable. The seed preview before the editor prompt is now rendered inside a bordered `rich.panel.Panel` titled "Seed commit message" so the message contents are visually distinct from surrounding console output (warnings, git status, prompts) and the user can see exactly which lines will be staged. 440 pytest pass; pyflakes clean.
- Audit fixes on `propagate/`: (1) updated `ROUTING_OVERRIDES` schema comment in `propagate/model.py` to include the `bucket` key (was incomplete); (2) added a note to `should_ship_override` docstring in `propagate/files.py` clarifying that the predicate evaluates gate fields only and that callers must re-read the rule dict to apply bucket overrides; (3) removed dead branch in `target_path_for_bucket` (`test_files` and fallthrough returned the same expression); (4) normalized `#============` section divider format in `propagate/model.py` (was inconsistently spaced); (5) replaced `typing.Optional[X]` annotations with `X | None` in `propagate/files.py` per `docs/PYTHON_STYLE.md` (also removed unused `import typing`); (6) clarified the `'tools'` entry comment in `META_DIRS` to explain it replaces the prior per-file `META_FILES` entry under the three-tier principle.
- Audit fixes on the changelog devel tools (post-rewire round 2): (1) pruned `tests/meta/test_commit_changelog.py` from 23 tests to 6 per Test auditor recommendation -- dropped 12 cleaner-primitive "regex tests its own example" cases plus 5 brittle constant-lock cases (`72`, `25`), keeping only the composition smoke for `clean_entry_text` and 5 branch-coverage cases for `make_seed_message_from_entries`; (2) added a module docstring to `devel/commit_changelog.py` (was missing); (3) fixed pre-existing style issues in `devel/commit_changelog.py`: `status_map.get(code, "modified")` -> explicit `if code in status_map` with `(raw)` fallback so unknown codes show the raw character instead of being mislabeled, `os.environ.get("GIT_EDITOR") or os.environ.get("EDITOR") or "nano"` -> explicit if-chain (the `or` chain would collapse empty strings and hide a real misconfiguration), and `raise SystemExit(rc)` -> `raise RuntimeError(...)` per PYTHON_STYLE.md "prefer raising errors"; (4) same `SystemExit(1) -> RuntimeError` fix applied to `devel/submit_to_pypi.py:fail()`; (5) renamed lowercase module globals `console`/`error_console` to ALL_CAPS `CONSOLE`/`ERROR_CONSOLE` in `devel/submit_to_pypi.py` per PYTHON_STYLE.md ALL_CAPS rule; (6) added `Returns:` clause to `changelog_lib.get_git_root` docstring; (7) added inline comment above `CONSOLE`/`ERR_CONSOLE` declarations in `changelog_lib.py` explaining the `highlight=False` rationale; (8) updated `docs/REPO_STYLE.md` automation bullet to reflect `changelog_lib`'s broader role (parser/serializer plus git helpers plus console + prompt helpers); (9) deleted dead `VERSION_RE` regex constant in `commit_changelog.py` (only used by removed helpers); (10) clarified the "phantom commit guard" comment in `main()` after Plan auditor flagged the original wording as misleading. Pyflakes clean across all 5 devel files; 136 tests/meta pass. Still deferred to follow-on work: bare `tuple`/`list`/`dict`/`set` type annotations across `changelog_lib.py` (pervasive, pre-existing across the file), dead `duplicate_policy='raise'` branch removal (only test exercises it, API touch), and import shortest-first ordering nits across multiple files (pre-existing repo-wide pattern).
- Audit fixes on the changelog devel tools (post-v2): (1) renamed module-level `console`/`err_console` to `CONSOLE`/`ERR_CONSOLE` in `devel/rotate_changelog.py` to match repo ALL_CAPS constant convention (and parity with `query_changelog.ERR_CONSOLE`); (2) replaced `glob.fnmatch.fnmatch` indirection with a direct `import fnmatch` in `devel/query_changelog.py`; (3) fixed stale inline rationale comments in `changelog_lib._is_valid_iso_date` that still referenced `datetime.date.fromisoformat` and a "calendar lookup that does not raise" even though the code uses a hardcoded month-length table; (4) tightened type hints on `current_category`/`bullet_title` in `changelog_lib.split_day_block` from `str` (with a `None` initial value) to `str | None`; (5) collapsed three overlapping inline comments in `query_changelog.resolve_categories` into one explanation of why the two error messages differ by shape; (6) rewrote the "parse entry date defensively (already validated upstream)" comment in `apply_filters` as a non-contradictory note about converting ISO string back to a `date` for comparison; (7) dropped the redundant explicit `duplicate_policy="warn"` kwarg at the only `parse_file` call site (matches the library default); (8) extended `docs/REPO_STYLE.md` "Changelog rotation" section with a closing bullet pointing readers at `devel/rotate_changelog.py` + `devel/query_changelog.py` + `devel/changelog_lib.py`; (9) rephrased the v1 changelog bullet's tail to acknowledge the same-day v2 extraction (per REPO_STYLE: entries are never removed but may be rephrased for accuracy). Pyflakes clean; both CLIs smoke-tested (`--dry-run` rotate; `--from 2026-05-20 -c fix` query). Deferred to follow-on work and not addressed here: high-severity `args._parser` stash in `query_changelog.py` (requires returning `(args, parser)`), `_is_valid_iso_date` rewrite to use the 2-line `try/except ValueError` form sanctioned by PYTHON_STYLE, dead `duplicate_policy='raise'` branch removal, and consolidating `run_git`/`get_git_root`/`ensure_in_git_repo` duplicated across `devel/commit_changelog.py`, `devel/rotate_changelog.py`, and `devel/query_changelog.py` into a shared `devel/git_helpers.py`.

### Decisions and Failures

- **Decision:** Rejected audit-suffixed `## YYYY-MM-DD` headings in the changelog (proposal was `## 2026-05-15 (audit)`). The changelog reader (`devel/changelog_lib.parse_file`), verifier (meta tests under `tests/meta/test_changelog_lib.py` plus the `parse_day_blocks` warning channel), rotator (`devel/rotate_changelog.py`), and commit helper (`devel/commit_changelog.py`) share one strict day-block contract: `## YYYY-MM-DD` only. Suffixes would create duplicate-day ambiguity and make rotation less predictable. Audit context belongs inside a bullet, not on the heading. Canonical forms: `- **Audit:** Verified changelog parser behavior after ...` under any existing category, or a dedicated bullet under `### Decisions and Failures` such as `- **Audit decision:** ...`. No code patch; `DATE_RE` in `devel/changelog_lib.py` stays strict.

## 2026-05-20

### Additions and New Features

- Split `propagate_style_guides.py` (was 2232 lines) into an entry orchestrator plus a `propagate/` helper package: `propagate/__init__.py` (empty), `propagate/console.py` (`log_action`, `print_summary`, counters), `propagate/model.py` (PropagateContext dataclass + all manifests/constants; zero local imports), `propagate/files.py` (filesystem I/O, gitignore block ops, merge helpers, `compute_propagation_plan`, `resolve_spec_for_type`, `auto_discover_test_files`), `propagate/repo.py` (repo discovery, REPO_TYPE read/write, git perms). The entry script keeps `parse_args`, `main`, `process_repo`, `exit_code_for`, plus orchestration helpers (`build_context`, `resolve_target_repos`, `collect_repo_dirs`, `remove_deprecated_tests`, `apply_file_bucket`). Import graph is a strict DAG: entry imports all helpers; `files` imports `console`+`model`; `repo` imports `console` only; `model` and `console` have zero local imports; no function-level cross-package imports anywhere. `reset_repo.py` removes both the entry script and the `propagate/` directory at consumer bootstrap (`git_rm("propagate_style_guides.py") + git_rm_recursive("propagate/")` at line 461). `propagate` added to `META_DIRS` (defense-in-depth alongside `tools/`, `templates/`, `meta/`) so the package never propagates into consumer repos. Test imports updated across `tests/meta/test_propagate_*` and `tests/meta/test_load_deprecation_lists.py` to point at new module locations; orchestration symbols continue to import from `propagate_style_guides`. All gates pass: `compileall` clean, `--help` matches pre-split output, `pyflakes propagate_style_guides.py propagate/*.py` clean, `pytest tests/` 375 passed, `reset_repo.py --dry-run` lists both removals for `--type python`, `--type typescript`, `--type rust`. CLI behavior, output, and exit codes unchanged byte-for-byte.

- (WP-H Patch 8) Added `copy_if_changed(source, dest, dry_run, action_label)` helper to `propagate_style_guides.py` to centralize the "is the destination byte-identical to the source?" check that previously appeared inline before every copy. Returns a tuple `(changed, action)` where `action` is one of `copied`, `updated`, or `unchanged`; callers route the result to the appropriate counter and log line. Dry-run mode short-circuits before any filesystem write. Snapshot gate byte-identical.
- (WP-H Patch 9) Added `apply_file_bucket(bucket_name, spec, repo_dir, repo_type, context, counters)` to `propagate_style_guides.py`, collapsing four near-duplicate per-bucket loops in `process_repo()` (overwrite/noexist/devel/test) into four single-line calls. The helper uses explicit per-bucket inline branches (no callback-dict indirection) so each bucket's distinct policy (noexist skip-if-present, devel new-file handling, test auto-discovery interaction) stays readable in place. Snapshot gate byte-identical.
- (WP-H Patch 10) Introduced a rich `Console(width=200)` plus an `ACTION_STYLES` table and a `log_action(tag, message)` function in `propagate_style_guides.py`. Migrated 49 color-tagged `print()` callsites to `log_action()` so every action line now flows through one styled writer with consistent tag formatting and column width.
- (WP-H Patch 11) Added `_QUIET_TAGS = {"NO CHANGE", "SKIP"}` and made `log_action()` suppress these tags unconditionally, incrementing per-repo counters instead of printing a line per file. Expanded `init_counters()` with the new fields `unchanged`, `skipped_source`, `skipped_self`, `skipped_path`, and `skipped_policy`; `validate_counters()` now requires every field; per-repo and global summary lines surface the new fields so the volume of detail is preserved in the summary.
- (WP-H Patch 12) Added `exit_code_for(counters)` helper and updated `main()` to return its result; the propagator now exits non-zero when `counters['errors'] > 0`. Previously `main()` always returned 0, masking real errors in CI.
- (WP-F0 Patch 5) Added orchestration helpers to `propagate_style_guides.py`: `build_context(args)` builds a typed `PropagateContext` dataclass from argparse args, centralizing path resolution and mirroring every field downstream helpers need; `resolve_target_repos(context)` returns either a single target repo (when `context.repo_name` is set) or discovered repos from `context.base_dir`; `discover_repos(base_dir)` wraps `collect_repo_dirs()` and applies the skip-list for consistent repo discovery; `init_counters()` initializes the mutable counter dict with all expected keys (copied_count, updated_count, merged_count, created_count, skipped_policy, auto_discovered_count, errors); `validate_counters(counters)` asserts all expected keys are present at end-of-run; `print_global_summary(counters)` owns the global summary line output. These helpers are standalone module-level functions; `main()` remains unchanged (Patch 6 will refactor main() to use them). Snapshot gate passes byte-for-byte; all 96 pytest tests green; pyflakes clean.
- Added `-v` / `--verbose-paths` CLI flag to `propagate_style_guides.py`. Default off shows the destination path as a repo-relative path; the flag toggles full absolute paths in every action line. Distinct from the deferred `--verbose` output-suppression flag (see Decisions and Failures entry for the boundary); this one controls path display only.
- Added `format_path_pair(src, dst, repo_dir, verbose)` helper to `propagate_style_guides.py`. Computes the repo-relative destination path and renders `src -> dst` only when the relative `src` and `dst` paths actually differ; otherwise renders the single path. Centralizes the action-line path rendering so the `-v` / `--verbose-paths` toggle has one place to gate on.

### Behavior or Interface Changes

- Replaced path-based `PYTHON_LANG_FILES` manifest with a routing-override table (`ROUTING_OVERRIDES` in `propagate/model.py`) and a `should_ship_override(file_rel, repo_lang, repo_dir)` predicate (`propagate/files.py`) that gates files on language and file-presence checks. Added `LANG_UNKNOWN`, `LANG_PYTHON`, `LANG_TYPESCRIPT`, `LANG_RUST`, `LANG_OTHER`, and `LANG_UNIVERSAL` constants. Updated `read_repo_type` to return `LANG_UNKNOWN` when the repo-type marker is missing and detection fails (was falling back to `'python'`), so unmarked repos no longer default to Python and do not receive Python-specific files (e.g., `docs/PYTHON_STYLE.md`). The override table gates `devel/submit_to_pypi.py` on the presence of `pyproject.toml` at the consumer, closing the leak where Python repos without a packaging intent received the PyPI submission script. Removed the inline `pyproject.toml` check from `apply_file_bucket` since the override predicate now handles it at plan-time. Behavior change: 'other'-typed and unmarked repos no longer receive `docs/PYTHON_STYLE.md` (that historical exception is now gone, consistent with the override table); language-gated files no longer ship for `LANG_UNKNOWN` repos.
- (WP-F1 Patch 6) Extracted `process_repo(repo_dir, context, counters)` function carrying all per-repo logic from `main()` loop body (lines 1481-1752, 271 lines). Function handles: `is_repo_dir` skip, self-skip/self-sync logic, `read_repo_type` call, `resolve_spec_for_type` call, `auto_discover_test_files` call, `ensure_git_perms` call, and four verbatim bucket loops (overwrite_files, noexist_files, devel_files, test_files) plus gitignore management (merge_gitignore_blocks, deduplicate_gitignore, remove_gitignore_entries) and per-repo summary output. Updated `resolve_target_repos(context)` to handle `--sync-self` insertion logic, prepending template_root to repo list when sync_self flag is set and template is not already present. Reduced `main()` to pure orchestrator: parses args, builds context, resolves repos, initializes counters, loops over repos calling process_repo, validates counters, prints summary, returns 0. Main body is 8 lines plus signature/docstring/return. Zero `args.X` references inside process_repo; all context passed via PropagateContext object. Snapshot gate passes byte-for-byte (parity verified); all 337 pytest tests green including 96 in tests/meta/; pyflakes clean. Every function responsibility summarizable in one sentence: process_repo orchestrates a single repo, resolve_target_repos resolves the target list including --sync-self logic, build_context constructs the orchestration context, init_counters initializes mutable tracking, validate_counters asserts completeness, print_global_summary outputs the final line.
- Added `templates/typescript/tools/html_to_pdf.mjs`, a base TypeScript CLI tool for rendering local HTML files and URLs to PDF using Playwright's `page.pdf()` API (Chromium-only). The tool accepts one required flag (`--input`) and two optional flags (`--output`, `--landscape`); all other settings are hardcoded for reproducibility (Letter format, 0.6-inch margins, screen media, 1440x1200 viewport, networkidle wait, backgrounds enabled, scale 1). Relative paths are resolved against the current working directory via `node:path` `resolve()` and `node:url` `pathToFileURL()`; HTTP/HTTPS and file:// URLs are passed verbatim to the browser. Exit code is non-zero on missing input flag, missing input file, or PDF generation error; runtime errors propagate with full stack (no defensive error wrapping). The tool lives in the TypeScript template tree (`templates/typescript/tools/`) and propagates to every TypeScript consumer repo via the existing propagation system.
- Extended `templates/typescript/docs/PLAYWRIGHT_USAGE.md` with a new `## PDF generation` section (35 lines) demonstrating npm and bare-node invocation styles, including a landscape example. The section documents the three hardcoded defaults, notes Chromium-only support, reminds existing consumers that they must manually add the `"pdf"` script and `playwright` devDependency (since `package.json.template` is marked `noexist`), and recommends staging PDFs to `/tmp/` or `test-results/` (already in `.gitignore`). Reason: the new tool serves three concrete v1 use cases (project reports, simple docs export, CI artifacts), but no use case requires the 9 flags that were initially audited (format, margins, media, viewport size, wait-until, background control, scale, headers/footers, multi-input merge).
- Added `templates/typescript/tests/playwright/test_html_to_pdf.mjs`, a node:test smoke test ensuring the new CLI tool runs end-to-end. The test writes a minimal HTML file (`<h1>PDF test</h1><p>x</p>`) under `os.tmpdir()`, spawns the tool via `spawnSync` with both absolute tmpdir paths and relative paths from `REPO_ROOT`, and asserts exit code 0, file existence, size > 1000 bytes, and %PDF magic bytes. Files are cleaned up in a `finally` block. Test lives under `tests/playwright/` so the existing `collect_ignore = ["e2e", "playwright"]` keeps it out of pytest collection, satisfying the test-naming convention that any file with a Playwright import must live under `tests/playwright/`.
- Externalized `DEPRECATED_TEST_SCRIPTS` and `DEPRECATED_GITIGNORE_ENTRIES` from `propagate_style_guides.py` to plain-text files under a new `meta/propagation/` directory (`meta/propagation/deprecated_tests.txt`, `meta/propagation/deprecated_gitignore.txt`). Added a `TEMPLATE_ROOT` module constant and a `load_deprecation_list(rel_path)` function that reads newline-delimited entries, skipping blank lines and comment lines starting with `#`. Missing files raise `FileNotFoundError` at import time (loud failure beats silent missing scrub). Goal: clearer organization and fewer hidden settings inside a 1500-line propagator script; reviewers can now inspect and update deprecation data without touching `.py`. The deprecation gitignore file carries a `# WARNING` header documenting that every entry is stripped from every consumer `.gitignore` on every run (no per-line origin sentinel) and that each new addition must be justified in `docs/CHANGELOG.md`. Routing constants (`META_FILES`, `META_DIRS`, `UNIVERSAL_NOEXIST`, `ROOT_PROPAGATE_ALLOWLIST`, `ROUTING_OVERRIDES`) stay in Python because they are part of `compute_propagation_plan()`'s algorithm. The `meta/propagation/` subtree is excluded from consumer propagation via the existing `'meta'` entry in `SKIP_WALK_DIRS` (line 105); also added `'meta'` to `META_DIRS` as defense-in-depth for `is_in_meta_dir()` checks. Location was selected after rejecting `propagation_meta/` (new naming pattern), `devel/propagation/` (devel/ already ships to consumers via the `devel_files` bucket), and `templates/meta/` (templates/ is reserved for typed overlays); `meta/propagation/` extends the existing `tests/meta/` convention without inventing a new tag. Dry-run parity verified: zero-line diff between `--dry-run --sync-self` output before and after the change (merged=50, copied=0, updated=0, skipped_policy=184). New regression test `tests/meta/test_load_deprecation_lists.py` adds 8 cases covering loader round-trip, indented-comment handling, real-file loads, entry-shape sanity (no path separators, no whitespace, non-empty), and propagation-plan exclusion across all repo types.
- Made `--output` flag optional in `templates/typescript/tools/html_to_pdf.mjs`; when omitted, the tool derives the output filename by replacing the input's extension with `.pdf` in the same directory (e.g. `report.html` -> `report.pdf`). Required `--input` remains required. If input is a URL (http/https/file scheme), `--output` is still required. If input already ends in `.pdf`, the tool exits with an error to prevent overwriting.
- Added `"pdf": "node tools/html_to_pdf.mjs"` to the npm `"scripts"` section of `templates/typescript/noexist/package.json.template` so existing consumers inherit the command once they propagate. Added `"playwright": "*"` to `"devDependencies"` in the same template; the bare `playwright` package (not `@playwright/test`) is the documented source for direct `chromium` launch (see the Packages subsection of PLAYWRIGHT_USAGE.md, lines 88-95). Existing consumers who inherit an older `package.json` must add both the script and dependency manually; the new doc section guides them on this point.
- (WP-H Patch 10) Consolidated the `SKIP SELF`, `SKIP SOURCE`, and `SKIP PATH` tag variants in `propagate_style_guides.py` into a single base `SKIP` tag with the qualifier moved into the message body. Reduces the active tag vocabulary and lets a single style entry in `ACTION_STYLES` cover every skip case.
- (WP-H Patch 11) Suppressed `NO CHANGE` and `SKIP` lines from default output. Detail is preserved via per-repo and global summary counters (new fields `unchanged`, `skipped_source`, `skipped_self`, `skipped_path`, `skipped_policy`); reviewers see one summary line instead of dozens of no-op lines per repo.
- (WP-H Patch 12) `propagate_style_guides.py main()` now returns non-zero when `counters['errors'] > 0`. Previously the process always exited 0; CI invocations could not detect propagator-level failures.
- Audit fix (post-Patch 12): `propagate_style_guides.py` now invokes `sys.exit(main())` so the computed exit code actually reaches the shell. The Patch 12 helper was correct, but the `__main__` guard discarded its return value, leaving CI silently green on errors.
- Audit fix (post-Patch 11): consolidated the two parallel skip counters into a single `skipped_policy` field. The legacy `skipped_by_policy` key was being incremented alongside the new granular field, double-counting every policy skip in the global summary.
- Audit fix (WS-E wiring): wired `copy_if_changed()` into the overwrite, devel, and test branches of `apply_file_bucket()`. The helper was defined in Patch 8 but every bucket still used the inline byte-compare; routing through the helper restores the intended unchanged/copied/updated routing.
- Audit-round-2 color UX reframe: `Console()` initialization changed to `Console(width=200, highlight=False)` to suppress rich's auto-syntax-highlighting of paths, numbers, and keywords (rendered as magenta on some terminals -- user feedback: "looks like pink lemonade"). `ACTION_STYLES` palette reframed per user feedback so routine actions (DRY RUN, COPIED, UPDATED, MERGED, CREATED, SKIP, NO CHANGE) now render dim; ERROR and REMOVED render bold red; WARN renders bold yellow. Section headings in summary blocks (GLOBAL SUMMARY header and per-repo summary anchor) render bold for scannability. Color philosophy: routine actions fade, exceptions and section headings draw the eye. These choices intentionally deviate from plan WP-G1's locked palette spec (which prescribed blue COPIED/UPDATED/MERGED, cyan SKIP, etc.); this CHANGELOG entry is the authoritative color record going forward.
- Audit-round-3 color UX final iteration: `log_action()` restructured to render mode prefix (`dry run`) and action verb as two separately styled tokens. Routine action verbs are color-accented (`update` yellow; `copy`, `merge`, `create` blue); the `dry run` mode prefix renders dim; path text renders plain. Reframed away from a verb-taxonomy palette toward an information-hierarchy model: color signals importance and structure, not verb identity. Supersedes the round-2 dim-everything palette where routine actions were uniformly faded.
- Adaptive SUMMARY block layout in `propagate_style_guides.py`. Single-repo runs now print only one SUMMARY block (which includes `repo` and `type` rows); multi-repo runs print per-repo summary blocks plus a final SUMMARY with a `repos` count row. Reduces redundancy in the common single-repo invocation where the per-repo block and the global block would otherwise repeat the same numbers.
- Dry-run mode counter rows now use forward-looking labels (`would update`, `would copy`, `would merge`, `would create`) instead of past-tense (`updated`, `copied`, `merged`, `created`). Matches the forward-looking intent of dry-run action lines, so the summary tense agrees with what the action lines said would happen.
- Path display in action lines now defaults to the destination path shown relative to the target repo. The `src -> dst` form is rendered only when the relative `src` and `dst` paths actually differ (e.g. rename or overlay placement); identical relative paths collapse to a single path. The new `-v` / `--verbose-paths` flag re-enables full absolute paths for debugging.

### Fixes and Maintenance

- (WP-I1 Patch 4) Extracted four single-purpose helpers in `propagate_style_guides.py` to reduce duplication and improve code clarity: `load_gitignore_block(path)` returns a list of stripped gitignore lines, filtering blanks and `#` comments (used by `compute_propagation_plan` and `merge_gitignore_blocks`); `write_repo_type_marker(path, token, dry_run=False)` writes the REPO_TYPE marker file via `write_text` (used by `read_repo_type` to avoid six inline marker-write patterns); `parse_repo_type_choice(text, default=None)` maps single-letter and full-word repo type inputs to canonical tokens, returning default on unknown input (used by `read_repo_type` to unify user-choice parsing across three prompts); `replace_managed_block(lines, header, block_lines)` replaces or appends a named managed block in a line list, idempotent across multiple calls (used by `merge_gitignore_blocks` to replace the find-block + slice + splice pattern). Updated `compute_propagation_plan()` to use `load_gitignore_block()` for both universal and typed gitignore sources (lines 520-523), eliminating parallel file-reading loops. Updated `merge_gitignore_blocks()` to use `load_gitignore_block()` and `replace_managed_block()` (lines 1151-1170), reducing the function from 107 to 74 lines. Updated `read_repo_type()` to use `write_repo_type_marker()` and `parse_repo_type_choice()` across six marker-write callsites and three user-choice prompts (lines 789, 803, 811, 829, 845, 851), improving maintainability and consistency. Added comprehensive unit tests in `tests/meta/test_propagate_helpers.py` (33 tests total across four test classes covering blank/comment filtering, missing-file behavior, all repo-type tokens, case insensitivity, whitespace handling, present/absent/multiple blocks, and idempotency). Snapshot gate and all 96 pytest tests pass; pyflakes clean.
- Removed try/catch wrapper around `parseArgs()` in `templates/typescript/tools/html_to_pdf.mjs` (lines 20-26). Defensive scaffolding around Node's own arg parser violates the no-try/except principle; let Node's natural TypeError propagate to stderr with full stack and exit non-zero.
- Removed unused `rmSync` import from `templates/typescript/tests/playwright/test_html_to_pdf.mjs` (was imported on line 9, never used).
- Replaced `Date.now()`-suffixed output filename (`test_output_relative_${Date.now()}.pdf`) with deterministic name `_pdf_relative_output.pdf` in the second test case, improving test reproducibility and enabling safe cleanup per repo-wide convention.
- Restored "/tmp/ or test-results/" output-path recommendation in `templates/typescript/docs/PLAYWRIGHT_USAGE.md` after the "Hardcoded defaults" line, clarifying which directories are safe for generated PDFs.
- Audit-pass-2 fixes to `test_html_to_pdf.mjs`: removed `.catch()` wrapper around `main()` in `html_to_pdf.mjs` so unhandled rejections surface with full stack; removed orphaned separator line; replaced REPO_ROOT try/catch fallback with direct import of centrally-propagated `repo_root.mjs`; added precheck that skips test with clear message when `tools/html_to_pdf.mjs` is absent (template repos only); removed unused `t` parameter from all three test cases; replaced 6 empty cleanup `catch` blocks with `existsSync` guards; replaced `assert.doesNotThrow(() => statSync(...))` with direct `existsSync` assertion; added `--output` URL-requirement one-liner to PLAYWRIGHT_USAGE.md.
- (WP-B1) Extracted four text and file mutation helpers in `propagate_style_guides.py` to centralize dry-run logic: `copy_file_safe(src, dst, dry_run, action='copy')` wraps `shutil.copy2()` and preserves executable bits; `make_dir_safe(path, dry_run)` wraps `os.makedirs(..., exist_ok=True)`; `read_text(path)` reads UTF-8 files; `write_text(path, content, dry_run, action='write')` writes UTF-8 files. All helpers use plain `print()` for dry-run messages to preserve snapshot output parity. Migrated 20+ inline dry-run/write patterns across 8 functions (`main()`, `ensure_changelog_file()`, `merge_conftest()`, `remove_gitignore_entries()`, `deduplicate_gitignore()`, `merge_gitignore_blocks()`). Unit tests under `tests/meta/test_propagate_helpers.py` (15 tests) cover dry-run vs real-run modes, executable-bit preservation for `copy_file_safe`, and round-trip invariants. Snapshot gate (`tests/e2e/e2e_propagate_dryrun_snapshot.py`) passes byte-for-byte; all 78 `pytest tests/meta/` tests green; `pyflakes` clean.
- (WP-D1) Unified `os.walk` filter logic in `propagate_style_guides.py` by extracting `safe_walk(root)` generator that applies `SKIP_WALK_DIRS` and dotdir (`.`-prefix) filters internally, mutating `dirs[:]` for traversal. Migrated three callsites: `compute_propagation_plan()` (line 287), `auto_discover_test_files()` (line 611), `ensure_git_perms()` (line 826). Each now uses `for dirpath, dirs, files in safe_walk(root):` form, eliminating inline filter duplication. All 63 `pytest tests/meta/` tests green; `pyflakes` clean.
- Audit fix: removed a `try/except ValueError` block in `process_repo()` per the no-try/except rule in `docs/PYTHON_STYLE.md`. Caller-side validation in `read_repo_type()` already guards the same condition; the inner wrapper masked the underlying design fault rather than fixing it.
- Audit fix: replaced misuse of `log_action("SKIP", ...)` for sync/discover diagnostics with plain `CONSOLE.print(...)`. The SKIP tag is reserved for the quiet-suppressed counter path; routing non-skip status lines through it produced double accounting and hid the diagnostics behind `_QUIET_TAGS` suppression.
- Audit polish: tightened type annotations on the new helpers, made the `_QUIET_TAGS` and `ACTION_STYLES` keys consistently `frozenset`/`dict[str, str]`, added a `PropagateContext` class docstring, and stripped now-stale "Phase A" plan labels from a handful of subsection headers and inline comments so the in-tree language matches the closed-plan vocabulary.
- Audit-round-2 noise + counter fix: `copy_if_changed()` was not threading `counters` through to `log_action()`, so `[NO CHANGE]` and `[SKIP]` lines kept printing on copy paths and the corresponding counters undercounted (observed: `unchanged=1` instead of `unchanged=21`). Added a required `counters: dict` parameter to `copy_if_changed()` and updated the three call sites in `apply_file_bucket`. WP-K1's "no `[NO CHANGE]`/`[SKIP]` in stdout" success criterion was not actually satisfied until this fix landed.
- Audit-round-2 HIGH self/source counter misroute: `apply_file_bucket` self-path guards in the overwrite, noexist, devel, and test branches were emitting `log_action("SKIP", f"source: {dest_file}", counters)`. The `"source:"` message prefix made `log_action` route to the `skipped_source` counter when the intent was `skipped_self`, so `skipped_self` always reported 0 in batch mode. Fixed by changing the prefix to `"self:"` at four call sites (lines 1631, 1675, 1710, 1739).
- Audit-round-3 BLOCKER fix: five `log_action()` callsites in `process_repo()` were still passing uppercase tag literals after the lowercase-key migration, raising `KeyError` on every hit. All five lived on hot paths and would have crashed under normal use: sync-self skip (line 1969), unknown REPO_TYPE error (line 1987), dry-run tests-dir mkdir (line 2024), conftest merge (line 2036), and conftest create (line 2039). Lowercased each tag to match the current `ACTION_STYLES` keys.
- Audit-round-3 fix: removed a `try/except` block from `format_path_pair()` that papered over a Windows path-edge case. The project targets macOS and Linux only, and the no-try/except rule applies; the suppressed exception was hiding a design bug rather than handling a real platform concern.

### Removals and Deprecations

- (WP-H Patch 10) Deleted the legacy `Colors` class from `propagate_style_guides.py`. Every callsite migrated to `log_action()` plus the `ACTION_STYLES` table; zero references remain. Inline ANSI escape handling is no longer in the module.
- Audit fix: dropped the redundant `skipped_by_policy` counter field from `init_counters()`, `validate_counters()`, and the global summary line. The Patch 11 granular field `skipped_policy` is now the sole survivor; downstream consumers reading the summary line must switch to the surviving name.
- Deleted `--sync-self` CLI flag and all sync_self logic from the propagator. The feature was dangerous: it processed the template root plus all discovered consumer repos in a single run, with a blast radius covering the entire base directory. The intended workflow (edit root copies, then sync to consumers) does not match user practice (users edit root directly). Template root now always self-skips; `propagate_style_guides.py` removed the `--sync-self` argument, the `context.sync_self` field from PropagateContext, the conditional processing logic in `resolve_target_repos()` and `process_repo()`, and all test cases exercising the feature. Updated `tests/meta/test_propagate_self_skip.py` docstring and test name to match new unconditional behavior. Removed 5 `sync_self=False` keyword arguments from context construction in test fixtures in `tests/meta/test_propagate_helpers.py`. Updated CHANGELOG entry gates line and `.gitignore` template comment to drop `--sync-self` references.
- Pruned cross-bucket links from universal Markdown files to enforce bucket isolation: removed 4 links from `docs/E2E_TESTS.md` and 2 links from `tests/TESTS_README.md` to the TypeScript-specific `../templates/typescript/docs/PLAYWRIGHT_USAGE.md`, replacing each with prose stating "TypeScript repos include a Playwright usage guide in the TypeScript overlay docs." Moved the "Template layout" section (lines 26-30) from `docs/REPO_STYLE.md` into `README.md` as a new "## Template layout" subsection, with the link path adjusted from `../meta/docs/PROPAGATION_RULES.md` to `meta/docs/PROPAGATION_RULES.md` to reflect the new root-level source location. The universal prose hint is sufficient; TypeScript consumers discover `PLAYWRIGHT_USAGE.md` by name in their propagated `docs/` after setup. Added `tests/meta/test_link_bucket_isolation.py` -- new template-only pytest test (in `tests/meta/`, excluded from propagation via `SKIP_WALK_DIRS`). Walks tracked `*.md` via `git ls-files`, classifies each file's bucket (universal / overlay-<type> / meta) using `propagate.model.META_FILES` and `propagate.model.META_DIRS`, resolves local links, and hard-fails on disallowed transitions. Allowed: universal->universal; overlay-X->universal or overlay-X (same type); meta->any. Skill bundles `computer-vision-expert` and `ui-ux-engineer` under `~/.claude/skills/` had their cross-bundle links to untracked book `.txt` files removed in a parallel patch; books now referenced by title only. Universal `docs/REPO_STYLE.md` retains the universal "Project type marker" section while dropping all references to template-meta files. Design rule: a file may only link to targets that propagate with it; universal docs link only to universal targets. Follow-up audit fixes: removed silent try/except in the new test, renamed `repo_root` -> `REPO_ROOT` per module-level constant convention, updated prose in `docs/E2E_TESTS.md` and `tests/TESTS_README.md` to name `PLAYWRIGHT_USAGE.md` explicitly instead of linking, added "Link bucket isolation" section to `meta/docs/PROPAGATION_RULES.md` documenting the rule and citing the enforcement test, and corrected `META_TEST_PREFIXES` documentation to note both `tests/meta/` directory-level exclusion and prefix-level exclusion mechanisms.

### Decisions and Failures

- **Deferred 8 optional flags after audit.** The CLI surface started at 9 candidate optional flags (`--format`, `--margin`, `--media`, `--viewport-width`, `--viewport-height`, `--wait-until`, `--no-background`, `--scale`) plus headers/footers and multi-input merge; all were cut to ship only `--landscape`. Rationale: base utilities should serve concrete use cases, not preemptive "someone might want to...". The three v1 use cases (reports, simple docs, CI artifacts) all fit within hardcoded Letter/0.6in/screen/1440x1200. Flags will be added (and documented in PLAYWRIGHT_USAGE.md) only when a real consumer delivers a use case that demands them. This decision mirrors the minimalism principle from docs/PYTHON_STYLE.md ARGPARSE section.
- **Corrected npm set-script form in doc and test design.** Initial doc example used the legacy `npm set-script pdf ...` form; npm v9 removed `set-script` in favor of `npm pkg set`. Corrected the new PLAYWRIGHT_USAGE.md section to recommend `npm pkg set` if consumers need to add the script manually, bringing the docs into alignment with current npm tooling.
- **Corrected test case for relative-path input.** Initial test design passed absolute tmpdir paths in all cases; revised to write the HTML file to REPO_ROOT (via a subdirectory inside the repo) and pass a relative path from REPO_ROOT, ensuring the test exercises the documented `node:path` `resolve()` code path and does not mask resolution bugs that would only surface when users invoke the tool from the repo root.
- **Rejected callback-dict abstraction for buckets (WP-H Patch 9 / WP-E1 acceptance).** Considered routing the four bucket loops through a `{bucket_name: handler_callable}` dict to remove per-bucket inline branching. Rejected in favor of explicit per-bucket inline branches inside `apply_file_bucket()` because each bucket has different policy (noexist skip-if-present, devel new-file handling, test auto-discovery interaction) that reads better in place than spread across four small handler functions and a registration dict. Matches the WP-E1 acceptance criteria.
- **Dropped `prompt_for_type` extraction (Phase A WP-C1 decision, restated for Phase B closure).** A standalone `prompt_for_type()` helper was on the Phase A list but was dropped after `read_repo_type()` already centralized prompting through `parse_repo_type_choice()`. Adding a second helper would have re-split logic that now reads in a single function.
- **No `--verbose` CLI flag for OUTPUT SUPPRESSION.** Quiet-by-default suppression of `NO CHANGE` and `SKIP` lines stays unconditional; detail is preserved in the per-repo and global summary counters. A `--verbose` flag aimed at re-enabling line-level no-op output was considered and deferred because no real consumer has asked for it. Revisit only when a real use case appears. Boundary clarification: the subsequently-added `-v` / `--verbose-paths` flag (see Additions entry above) is a distinct concern -- it toggles PATH DISPLAY (repo-relative vs full absolute paths in action lines) and does NOT re-enable suppressed `NO CHANGE` / `SKIP` lines. The two flags address different problems and do not contradict.

### Developer Tests and Notes

- Refined `devel/changelog_lib.py` warning shapes and added `DayBlock.lead_text` based on findings from the changelog corpus survey. The old generic `bullets before any category; using Uncategorized` warning is replaced by three precise shapes: per-block `legacy flat block (no category heading)` for blocks with zero `### Category` headings; per-block `orphan bullets before first category heading` for the rarer case where a category exists but bullets precede it; and a new per-block `lead text under day heading: <excerpt>` whenever a non-bullet, non-heading line sits between the date heading and the first bullet/category (captures author-attribution lines such as `Neil Voss <vossman77@yahoo.com>` and `OpenAI Codex` that were previously silently dropped by entry-view consumers). `parse_file` post-processes the per-block `legacy flat block` warnings into a single per-file summary line `legacy flat changelog: N day blocks have bullets with no category heading (treated as Uncategorized)`, so a file with 33 bare-flat blocks produces 1 warning instead of 33. `DayBlock` gains a new defaulted `lead_text: str = ""` field populated by `parse_day_blocks`; the `raw_text` round-trip is unchanged, so the rotator is unaffected. `devel/query_changelog.py` adds `uncategorized`/`uncat` to its category alias map (so `--category uncat` filters legacy entries directly) and renders captured `lead_text` as `> ` blockquote lines under date headings in text output (JSON and CSV unchanged: lead text is human context, not entry data). Analyzer at `meta/tools/analyze_corpus.py` updated to track the three new warning shapes, source the lead-text frequency table directly from `DayBlock.lead_text` instead of re-parsing warning text, and prepend a `## Headline` verdict paragraph to `report_changelog_corpus.md`. New `tests/meta/test_changelog_lib.py` cases (six total) cover the warning-shape split, lead-text capture (with and without a category in the same block), and the per-file `LEGACY_FLAT` collapse. Gates: 459 pytest pass; pyflakes clean; `rotate_changelog.py --dry-run` output unchanged; analyzer regenerated baseline: 52 files / 452 blocks / 10,416 entries / 56 warnings (down from 213 thanks to the per-file collapse) with bucket counts 27 `legacy_flat_summary`, 18 `duplicate_date`, 11 `lead_text_under_heading`, 0 `bullets_before_first_category`, 0 `bad_date`. Plan archived at `/Users/vosslab/.claude/plans/for-a-follow-plan-bright-flute.md`.
- Added `tests/meta/test_changelog_corpus_compat.py` plus a frozen corpus under `tests/fixtures/changelog_corpus/` (52 real-world `CHANGELOG*.md` files captured from `~/nsh/*/docs/`, flattened with `__` separators). Read-only tolerance audit: runs `changelog_lib.parse_file(path, strict=False)` against every fixture, asserts no crash, asserts at least one parsed `DayBlock` when a `## YYYY-MM-DD` heading is present, and prints a per-file report plus an aggregate warning-bucket summary (`bad_date`, `duplicate_date`, `non_canonical_category`, `uncategorized_bullets`, `no_headings_in_file`, `other`). Warnings are reported, never asserted on; this is a parser-tolerance survey, not a style verifier. Baseline survey on the captured corpus: 52 files surveyed, 452 day blocks parsed, 10,415 entries, 213 warnings (18 duplicate_date, 195 uncategorized_bullets, 0 bad_date, 0 non_canonical_category, 6 files with no date headings at all). Excluded the new fixture dir from `tests/test_ascii_compliance.py` (added `changelog_corpus` to `SKIP_DIRS`), `tests/test_whitespace.py` (same), and `tests/test_markdown_links.py` (added `SKIP_PATH_SUBSTRINGS` filter) so the inert fixture content is not rewritten or linted as repo-owned markdown. Full pytest pass (502 cases). Plan archived at `/Users/vosslab/.claude/plans/for-a-follow-plan-bright-flute.md`; follow-on (separately scoped): decide which warning shapes from the bucket summary justify targeted cleanup in sibling repos.
- (WP-H Patch 10 and Patch 11) Regenerated the propagator snapshot fixtures twice during Phase B: once for the SKIP tag consolidation in Patch 10, once for the quiet-by-default line suppression in Patch 11. Each regeneration was preceded by a reviewed diff to confirm only the intended output changed.
- (WP-H Patches 8, 9, 12) Added 15+ new unit tests under `tests/meta/test_propagate_helpers.py` covering `copy_if_changed` (7 cases: source-missing, dest-missing, byte-identical short-circuit, content-differs, dry-run path, action-label routing, executable-bit preservation), `apply_file_bucket` per-bucket behavior (5 cases: one per bucket plus a cross-bucket interaction), and `exit_code_for` (3 cases: zero counters, errors > 0, mixed counters with errors > 0). Final Phase B gates: 352 pytest pass, pyflakes clean, snapshot harness exit 0.
- Audit fix: renamed `test_*` helper functions to `check_*` inside `tests/e2e/` harness files. Pytest's `collect_ignore = ["e2e", "playwright"]` already skips that subtree, so the `test_*` prefix was silently misleading; the rename matches the naming convention enforced by `tests/test_test_naming_conventions.py`.
- Audit fix: replaced inline counter-dict literals scattered across unit tests with calls to the `init_counters()` factory, eliminating drift between the test fixtures and the runtime counter shape (the missing-`skipped_policy` field caught during the audit was a direct consequence of duplicating the dict by hand).
- Audit fix: deleted thin stdlib-wrapper tests (`test_read_text_round_trip`, `test_make_dir_safe_creates_parent`) that exercised `pathlib`/`os.makedirs` more than they exercised our helper logic. The remaining `test_propagate_helpers.py` tests cover the behavior the helpers actually add (dry-run skip, executable-bit preservation, action-label routing).
- Audit-round-2 snapshot harness path normalization: improved `normalize_paths()` in `tests/e2e/e2e_propagate_dryrun_snapshot.py` to strip machine-absolute paths from the per-repo summary line as well as the body lines (previously only body lines were normalized). Snapshot fixtures regenerated a third time in Phase B; fixtures are now portable across machines and safe to commit.
- Audit-round-3 snapshot regeneration: snapshot fixtures regenerated a fourth time in Phase B to absorb the color-rendering changes from the 2-token `log_action()` render plus the counter-routing fix for the five lowercase-tag `KeyError` bugs. Original plan budgeted two regenerations in Phase B; corrected count is now four (originals: Patch 10 tag consolidation, Patch 11 line suppression; round-2 noise + path normalization; round-3 color render + KeyError fix).
- Audit-round-3 snapshot harness portability: replaced a hardcoded `/Users/vosslab/...` repo root in `normalize_paths()` in `tests/e2e/e2e_propagate_dryrun_snapshot.py` with a call to `file_utils.get_repo_root()`. The harness is now portable across machines without per-machine fixture regeneration.

## 2026-05-19

### Additions and New Features

- Added `tests/test_readme_first_paragraph.py`, a fast pytest enforcing the three GitHub About rules from `docs/REPO_STYLE.md` `## README.md and GitHub About descriptions`: the first paragraph of `README.md` must exist, must be 250 Python characters or fewer (`len(paragraph)` after soft-wrap lines are joined with single spaces, matching GitHub's render of a paragraph), and must not contain the repo name verbatim. Helper `_load_first_paragraph()` walks the file's blank-line-separated blocks and skips leading heading-only blocks and badge-only blocks (image/link-only rows between the title and prose), then within the first prose-bearing block also drops any leading lines beginning with `#` so READMEs that put the title and first paragraph on adjacent lines without a blank separator still resolve correctly. The repo-name check strips Markdown link URLs first (only visible link text is inspected) and backticks (so a code-formatted name still counts), then case-insensitive substring-matches against both the dash and underscore spellings of `os.path.basename(REPO_ROOT)`. Each rule lives in its own `test_*` function so failures point directly at the violated rule, and the length-failure message reports the actual character count, the limit, and the full paragraph text. Reuses `file_utils.get_repo_root()` per the established repo helper convention. Reason: nothing previously verified that the README first paragraph could be copied verbatim into the GitHub About field; drift was only visible to a human at copy time.
- Extended `tests/test_readme_first_paragraph.py` from three checks to six, covering the full set of "good README" structural rules. Added `test_readme_exists` (file must be present at repo root), `test_readme_has_single_h1_title` (first Markdown heading must be a single `# Project Name` line and the file must contain exactly one level-one heading; fenced code blocks are skipped so example `# ...` lines inside code do not count), and `test_first_paragraph_is_plain_prose` (rejects bullet/numbered list markers at line starts and rejects paragraphs that are only badges/links once Markdown links and images are stripped). Helper `_read_readme_text()` centralizes file I/O. A placeholder-phrase scan (TODO/TBD/coming soon/no description provided) was prototyped and rejected as too fragile: legitimate `[docs/TODO.md](docs/TODO.md)` links and code examples in nearby blocks produced false positives, and the filename/link/code-span exclusions required to suppress them made the rule brittle without catching meaningful new failures. Reason: the original three-check version only guarded the GitHub About copy text; the new three checks guard against the other common "broken README" shapes (missing file, multiple/promoted H1s, bullet-list first paragraph) so a single `pytest tests/test_readme_first_paragraph.py` run answers "is the README in good shape".
- Rewrote the first paragraph of `README.md` to satisfy the new `tests/test_readme_first_paragraph.py` checks and the structure rule from `docs/REPO_STYLE.md` (`[What it is] + [who/use case] + [distinctive detail]`). Previous text was 262 characters and led with the backticked repo name (`` `starter_repo_template` is canonical bootstrap infrastructure for Python repositories... ``); new text is 228 characters, drops the repo-name self-reference, and leads with the purpose ("Canonical bootstrap scaffolding for new Python repositories") followed by the distinguishing details ("ready-to-use repo policy docs, Python style rules, licensing boundaries, and pytest lint checks"). The paragraph now fits the GitHub About field verbatim. The "Only `README.md` and `docs/CHANGELOG.md` are intentionally repository-specific" sentence, the `## Documentation` index, and the `## Quick start` section are unchanged.

### Fixes and Maintenance

**Pass-2 audit fixes (template-level quality):**
- **HIGH**: META_FILES path-form bug fix in `compute_propagation_plan` (was checking bare basename only; `docs/PROPAGATION_RULES.md` and `tools/detect_repo_type.py` were leaking to consumers); regression tests added.
- reset_repo.py `main()` refactored 308 -> 102 lines; 11 module-level helpers with `#====` separators + phase comments.
- tests/meta/ purged 11 subprocess CLI tests (19 -> 8 files); kept direct-import pure-function tests; runtime improved.
- Deprecated gitignore scrubbing wired into main propagator loop (`remove_gitignore_entries()` was defined but never called); scrubs 14 deprecated patterns from consumer `.gitignore`.
- reset_repo.py: write_marker action-count fix (was 0, now 1).

**Earlier pass-1 fixes:**

- WP-F3: Reverted `propagate_style_guides.py` architecture from universal-template-only to per-repo-type dispatching. Restored the five-bucket propagation system (`TYPED_SPEC['python|typescript|rust|other|universal']` with type-specific overwrite/noexist/test/devel/gitignore blocks). Depth-3 git discovery (.git up-tree search) scans consumer repos for `REPO_TYPE` marker; when found, invokes propagator with the detected type. Added `--base-dir` / `--fix-permissions` / `--no-auto-discover` flags (plan-specified count: 4 flags, 1 boolean each). Propagator skips template root itself (skip-list includes `starter-repo-template` and `vosslab-skills`). Remedies the architecture regression from WP-F1 where the propagator lost type dispatch; per-repo propagation now respects the project type, not universal defaults.
- WP-G1: Rewrote `reset_repo.sh` as `reset_repo.py` (Python-based bootstrap). Prompts (or accepts flags) for project type + SPDX code/docs licenses; writes `STARTER_REPO_TYPE` marker; copies selected licenses from `LICENSES/` catalog; invokes propagator with `--bootstrap` + `--no-auto-discover` flags; truncates README and CHANGELOG stubs; removes propagator and itself. Supports `--type / --code-license / --docs-license / --non-interactive / --yes / --dry-run / --commit / --no-stage / --force` flags (all but last three were plan-specified). Replaces legacy shell bootstrap with Python control flow. Removes bootstrap one-shot scripts after first use (normal pattern: clone -> run reset_repo.py -> commit -> develop).
- WP-F4: Excluded template-meta tests (`test_propagate_*`, `test_reset_repo_*`, `test_detect_repo_type`) from consumer propagation via `META_TEST_PREFIXES`; added `tests/test_propagate_meta_test_isolation.py` to verify the exclusion.
- WP-F5: Added `tools/detect_repo_type.py` for repo type prediction (Cargo.toml/tsconfig.json/pyproject.toml/file-count signals); propagator `--write-marker` flag + reset_repo.py use the prediction as a prompt default; 13 tests under `tests/meta/test_detect_repo_type.py`.
- Marker rename `STARTER_REPO_TYPE` -> `REPO_TYPE` across 24 consumer repos + template + all code/test/doc references; legacy fallback in `read_repo_type()` with `[WARN]` log.
- WP-F6 + addendum: Replaced TYPED_SPEC dict with folder-convention `compute_propagation_plan()`; 4 thin manifests (`ROOT_PROPAGATE_ALLOWLIST`, `UNIVERSAL_NOEXIST`, `PYTHON_LANG_FILES`, `META_FILES`/`META_DIRS`); precedence rules documented; `[OVERLAY-OVERRIDE]` log line added; new `docs/PROPAGATION_RULES.md`; `package.json.template` moved to `templates/typescript/noexist/`; `gitignore.universal` moved to `templates/`; root `.gitignore` restructured with UNIVERSAL+LOCAL managed blocks.
- Moved 19 template-meta pytest files from `tests/` to `tests/meta/` so root `tests/` stays purely universal hygiene.
- Moved `detect_repo_type.py` to `tools/detect_repo_type.py`; reset_repo.py git-rm's `tools/` at consumer bootstrap; `META_FILES` updated.
- Gitignore trim: `templates/gitignore.universal` reduced to 3 entries (.DS_Store, report_*.txt, _temp*.?*); `templates/python/gitignore.python` bare-bones (no __pycache__, no venvs - user wants visible for manual cleanup); `templates/rust/gitignore.rust` drops Cargo.lock (per-binary decision); `templates/other/` removed.
- Bug fix: `--sync-self` now properly includes template repo in iteration (skip-list was excluding it).
- Removed legacy `reset_repo.sh` (Python rewrite fully active).

## 2026-05-14

### Additions and New Features

- Added `tests/test_markdown_links.py`, a repo-wide pytest that checks every local Markdown link in tracked `.md` files. For each inline link or image (skipping links inside fenced code blocks and inline code spans) it enforces: filesystem-absolute links are hard errors (`~/`, `file://`, Windows drive letters, and leading-slash paths whose first segment is a system root like `home`/`Users`/`var` -- these only work on one machine, never on github.com); the resolved target must stay inside `REPO_ROOT` (no `..` escapes); the target must be a tracked file (untracked-but-present files pass locally yet 404 on GitHub); `..` traversal is rejected when a `..`-free path to the same target exists; and path-like link text (text containing `.md` or `/`) must equal the URL or its basename, while descriptive text like `[Design philosophy](REPO_STYLE.md#core-philosophies)` is allowed. A leading `/` is treated as repo-root-relative (GitHub behavior), so `[/docs/X.md](/docs/X.md)` stays valid. Failures are written to `report_markdown_links.txt` and raise `AssertionError`, mirroring `tests/test_ascii_compliance.py`. Reason: nothing previously verified that cross-doc links resolve or follow the repo link-style rules; the core goal is that every Markdown link works when browsed on github.com.

### Fixes and Maintenance

- Fixed three real link-text bugs surfaced by the new test. `docs/CLAUDE_HOOK_USAGE_GUIDE.md` and `docs/REPO_STYLE.md` had sibling links inside `docs/` whose visible text carried a wrong `docs/` prefix (`[docs/REPO_STYLE.md](REPO_STYLE.md)`); corrected the text to the bare filename to match the URL.
- Backtick-wrapped the three example links in the "When referencing files" bullet of `docs/REPO_STYLE.md` (the `good:`/`bad:` examples and the literal `[link text](URL)` syntax example) so they render as inline code and are excluded by the link checker's code-span skip. Consistent with `docs/MARKDOWN_STYLE.md`, which already backticks its link examples.
- Cross-referenced the new lint test from where its rules live: added a one-line pointer to `tests/test_markdown_links.py` in the "Scripts and executables" lint-test inventory of `docs/REPO_STYLE.md` (alongside `test_pyflakes_code_lint.py` and `test_ascii_compliance.py`), and added a one-line pointer at the end of the `## Links` section of `docs/MARKDOWN_STYLE.md` so a reader of the link rules can see the enforcer's name.
- Replaced an inline `lambda` in `tests/test_markdown_links.py` with a named `blank_match` helper, per `docs/PYTHON_STYLE.md` (`lambda` only allowed as `key=` for `sorted`/`min`/`max` with a short obvious expression).
- Loosened two `tests/test_markdown_links.py` rules after running the check against a larger sibling repo (vosslab-skills) surfaced two false-positive patterns. (1) Existence check now accepts **directory** targets as well as file targets: a new `build_tracked_dirs` helper derives the set of every directory containing any tracked file, so links like `[skills/](../skills/)` pass because GitHub renders directory links as a directory listing. (2) Path-like-text check now accepts any tail suffix of the target's repo-root-relative path components, not just the basename or the exact URL. For target `skills/x/y.md`, all of `y.md`, `x/y.md`, and `skills/x/y.md` are valid visible text. This tolerates verbose-but-correct forms like `[skills/foo/bar.md](../skills/foo/bar.md)` from a sibling subtree while still catching genuine mismatches where the text path components do not appear in the target. Side effect: `[docs/REPO_STYLE.md](REPO_STYLE.md)` (text shows the repo-root path of the target it correctly points to) now passes; only links whose text and URL would resolve to **different** files are flagged. Text and URL are compared after stripping a trailing slash so `[tests/]` matches a `tests` directory target.

## 2026-05-09

### Behavior or Interface Changes

- Clarified headless behavior in `docs/PLAYWRIGHT_USAGE.md`. Added a `## Headless by default` section between `## Script template` and `## Common patterns` stating that the canonical `chromium.launch()` with no arguments already defaults to `headless: true`, so existing scripts run headless without intervention. Codified the agent rule explicitly: do not pass `headless: false`, do not add `--headed`; the default is correct. Reserved the override for humans who explicitly want to watch a local debug session. Reason: agents have been "helpfully" overriding the default and flooding the desktop with browser windows during otherwise-clean CI-style runs. Added a matching Troubleshooting row ("Browser windows pop up during test runs") so the symptom maps back to the rule.

## 2026-05-08

### Additions and New Features

- Added **Finish the obvious** to `docs/REPO_STYLE.md` as a fifth core philosophy, clarifying that agents should continue through obvious safe follow-on work (defined by the plan, implied by the current task, or required to verify the work) and stop only at real blockers: missing information that cannot be inferred, a risky or irreversible action, or work that changes the user's requested outcome. Updated the section intro from "Four principles" to "Five principles". Pairs with "Atomic task decomposition": end-of-atomic-task is a checkpoint, not a stop, when the next atom is defined and unblocked.
- Added a `## Core philosophies` section to `docs/REPO_STYLE.md` naming four canonical principles that were previously implicit or absent in the repo: "Long-term over short-term", "Fix the design, not the symptom", "Fresh subagent per task", and "Atomic task decomposition". REPO_STYLE.md is the single canonical home for all four; sibling docs (`docs/PYTHON_STYLE.md`, `docs/PYTEST_STYLE.md`, `docs/TYPESCRIPT_STYLE.md`) thread short cross-references back rather than duplicating prose, and `AGENTS.md` carries only a one-line operational pointer. Working rule: one canonical definition, many short cross-references. The first two principles were already encoded implicitly (no try/except, no defensive defaults, "delete fragile tests rather than rewrite") but were never named; the two execution principles were entirely new. This set is intended to serve as the canonical foundation for later skill-corpus cleanup; downstream skill and propagated-doc work should apply these principles, not redefine them.
- Added a new `## AGENTS.md files` section to `docs/REPO_STYLE.md` codifying as a repo rule that `AGENTS.md` should be concise and operational (target 100-150 lines), focused on specific tasks/workflows/constraints, and must not host long philosophical prose or duplicated style guidance. Canonical explanations live in `docs/*.md` and are linked from `AGENTS.md`. Reason: concise `AGENTS.md` files help coding agents scan, prioritize, and follow instructions more reliably.

### Behavior or Interface Changes

- Added `tests/test_test_naming_conventions.py` to lint the test folder layout. Five rules: no `test_*.py` under `tests/playwright/` or `tests/e2e/` (collect_ignore would silently skip them, mismatching the name); Python files in `tests/e2e/` must use the `e2e_*.py` prefix; shell files there must use `e2e_*.sh`; any `.mjs` file with a Playwright import must live under `tests/playwright/`.
- Added `tests/TESTS_README.md` as a navigational quick-reference to the test folder layout, how to run each tier, the `collect_ignore` guard, and the difference between the Playwright tier and the non-browser E2E tier. Cross-links the deeper docs.
- Added `tests/conftest.py` (was empty) with `collect_ignore = ["e2e", "playwright"]` so pytest does not collect anything in those subtrees regardless of filename.

### Behavior or Interface Changes

- Added a one-line changelog-mode rule to `AGENTS.md` `## Coding Style`: standalone edits update `docs/CHANGELOG.md` directly; manager-driven multi-subagent work assigns one docs subagent to write a consolidated changelog entry, keeping the changelog coherent and avoiding parallel-edit churn.
- Reframed the intro of `## DO NOT HIDE BUGS WITH DEFAULTS` in `docs/PYTHON_STYLE.md` to name the umbrella principle ("fix the design, not the symptom") and link to its canonical home, so the section's role as the Python expression of that philosophy is on the page rather than implicit.
- Adopted a two-tier test layout convention to replace the prior `tests_e2e/` top-level folder. Browser-driven tests (Playwright today) live in `tests/playwright/`, with an optional `tests/playwright/e2e/` sub-grouping for full-path walkthroughs. Non-browser whole-system E2E (CLI round trips, build pipelines, shell or Python orchestration) lives in `tests/e2e/`. Reason: Playwright is a tool, E2E is a scope; not every Playwright test is end-to-end (a layout or single-interaction smoke check is browser-driven but not E2E). Naming the folders by execution model (browser vs. non-browser) keeps each test discoverable by what it actually is. Updated `docs/E2E_TESTS.md`, `docs/PLAYWRIGHT_USAGE.md`, `docs/PYTEST_STYLE.md`, `docs/PYTHON_STYLE.md`, `AGENTS.md`, and `README.md` to match.
- Pytest fast-lane safety now lives in active config: `tests/conftest.py` declares `collect_ignore = ["e2e", "playwright"]`. The filename conventions inside each subfolder remain as a readability layer on top of the active guard, not as the safety mechanism. Note: `collect_ignore` only affects pytest test collection; the repo's other lint tests (ASCII compliance, whitespace, pyflakes, etc.) still scan files in those subfolders via `git ls-files`.

### Fixes and Maintenance

- Extended `propagate_style_guides.py` to ship the new Playwright/test-tier infrastructure to consumer repos in three related changes. (1) Registered `tests/TESTS_README.md` (overwrite-always, matching its role as a centrally-maintained navigational README) by adding it to `TEST_SCRIPTS` with an inline comment explaining why a `.md` doc lives in a list otherwise full of test scripts. (2) Registered `devel/setup_playwright.sh` (added to `DEVEL_SCRIPTS`) and `tests/playwright/repo_root.mjs` (added to `TEST_SCRIPTS` as `playwright/repo_root.mjs` -- the subdir prefix is the new convention for tests/ subfolder entries, documented in `build_source_maps`). The test-script copy loop now mkdirs the destination parent so subdir entries land correctly. (3) Added `merge_conftest()` so the propagator injects `collect_ignore = ["e2e", "playwright"]` into each consumer's `tests/conftest.py` when missing -- mirroring the `merge_claude_md` pattern: the canonical line is added, but any custom content (imports, fixtures, hooks) the repo has is preserved. Empty conftests get the source verbatim; non-empty conftests with no `collect_ignore` token get the canonical block prepended; conftests that already have any `collect_ignore` line are left alone. New `updated_conftest` counter surfaces in the summary. Also updated `docs/PLAYWRIGHT_USAGE.md` to document `setup_playwright.sh` (quick install) and `tests/playwright/repo_root.mjs` (shared helper, do not edit per-repo), and updated `tests/TESTS_README.md`'s layout diagram to list `repo_root.mjs`. Verified via `--dry-run`: all 28 consumer repos receive the new files; 26 conftests get `collect_ignore` injected and 1 is created from scratch; source repo is correctly skipped throughout.
- Applied review-driven cleanup to the same change. In `propagate_style_guides.py`, removed an unreachable inner branch in `merge_conftest`'s empty-dest path (source always contains `collect_ignore`, so it can never equal an empty dest); switched the prepend path from `dest_text.lstrip()` to verbatim `dest_text` so a consumer repo's leading license header or blank lines survive the merge; added comments on the missing-dest early return, the source-repo abspath guard, and the subdir mkdir block. In `docs/PLAYWRIGHT_USAGE.md`, fixed a stale `collect_ignore = ["playwright"]` snippet (now `["e2e", "playwright"]`, matching `tests/conftest.py`) and replaced the repo-specific `cell_culture_game.html` example with a generic `index.html`. In `tests/playwright/repo_root.mjs`, added a comment explaining why `.trim()` is required (git rev-parse appends a trailing newline that would break path joins).

### Decisions and Failures

- Considered three competing layouts before settling on two distinct top-level folders. Rejected `tests_e2e/` (extra top-level), `tests/e2e/` as the single home for Playwright (conflated tool with scope, mislabeled smoke and layout checks as E2E), and `tests_browser/` (extra top-level). Adopted `tests/playwright/` plus `tests/e2e/` because each folder names what unifies its files. Initial dispatches landed `tests/e2e/` as the single Playwright home and had to be reverted before final docs landed.

## 2026-05-06

### Additions and New Features

- Added `docs/E2E_TESTS.md` documenting the convention that end-to-end tests live in `tests_e2e/` outside pytest. Pytest stays the fast lane under `tests/`; E2E scripts run via their own shell or Python runners. Cross-linked from `docs/PYTHON_STYLE.md` and `docs/PYTEST_STYLE.md`.

### Behavior or Interface Changes

- Established a hard boundary on `assert` placement: `assert` statements are forbidden in plain scripts and library modules and must live only in `tests/test_*.py` or in `tests_e2e/` end-to-end scripts. Reason: module-level asserts run at import time and slow script startup. Rewrote the `## ASSERT` section in `docs/PYTHON_STYLE.md` (removed inline-assert examples), added a "No `assert` in plain scripts" bullet to `## Common misconceptions`, and updated `docs/PYTEST_STYLE.md` `## Test structure` to allow asserts in `tests/` and `tests_e2e/` and to point slow tests at `docs/E2E_TESTS.md`.
- Added `@docs/E2E_TESTS.md` to `CLAUDE.md` so agents auto-load the E2E rule.
- Updated `AGENTS.md` to note that slow E2E tests live in `tests_e2e/` and run outside pytest.
- Updated `README.md` Quick Start to split fast (`pytest tests/`) and slow (`bash tests_e2e/run_all.sh`) test commands, and listed `docs/PYTEST_STYLE.md` and `docs/E2E_TESTS.md` in the Documentation section.
- Added a GitHub-link compatibility rule to `docs/MARKDOWN_STYLE.md` `## Links`: relative URLs must resolve against the file containing the link (not the repo root). Documented when to use the `docs/` prefix (linking from repo root into `docs/`) versus a bare filename (linking between siblings inside `docs/`).
- Removed an orphaned editor note from `docs/PYTHON_STYLE.md` that read "Here is a tightened version that keeps the rule and examples, without extra explanation."
- Split pytest-specific testing guidance out of `docs/PYTHON_STYLE.md` into the new `docs/PYTEST_STYLE.md`, matching the existing pattern of separate tool and language docs.
- Replaced duplicated pytest failure triage details in `docs/REPO_STYLE.md` with a link to `docs/PYTEST_STYLE.md`.
- Clarified `docs/PLAYWRIGHT_USAGE.md` so Playwright `.mjs` scripts live in `tests/`, can coexist with pytest `test_*.py` files, and follow the `docs/TYPESCRIPT_STYLE.md` testing convention.
- Updated `docs/PYTEST_STYLE.md` to make `conftest.py` the home for pytest setup and set `pytest tests/` as the default pytest command.
- Standardized repo-owned pytest command examples to use `pytest tests/`, while keeping `source source_me.sh && python...` for normal Python script execution.
- Documented `docs/CLAUDE_HOOK_USAGE_GUIDE.md` as a generated hook behavior reference instead of a repo style source of truth.
- Added `docs/PYTEST_STYLE.md` to the centrally maintained docs list in `docs/REPO_STYLE.md`.
- Added rule to `docs/PYTEST_STYLE.md` against creating permanent pytest files for temporary or scratch code (`_temp.*`, debugging scripts); `tests/` is reserved for code that stays in the repo.

## 2026-04-18

### Behavior or Interface Changes

- `propagate_style_guides.py` now auto-discovers `docs/*.md` files in the source repo and propagates them with overwrite semantics, mirroring the existing `test_*.py` auto-discovery in `tests/`. Picks up `docs/PLAYWRIGHT_USAGE.md` and `docs/TYPESCRIPT_STYLE.md` without needing to edit `STYLE_FILES`. `AUTHORS.md` and `CHANGELOG.md` are excluded via the new `AUTO_DISCOVER_DOCS_EXCLUDE` set so they remain per-repo.
- `docs/PLAYWRIGHT_USAGE.md` now directs Playwright test scripts to `tests/` at the repo root instead of `devel/`, matching `docs/TYPESCRIPT_STYLE.md` and `docs/REPO_STYLE.md` pytest conventions. `devel/` is reserved for one-off developer tools, not bulk test files. Python and Node test files may coexist in `tests/`.
- Expanded the Install section of `docs/PLAYWRIGHT_USAGE.md` to show `npm init -y` and `npm install --save-dev playwright` for repos that do not yet have a `package.json`, so setup from a fresh clone is explicit.

## 2026-04-01

### Additions and New Features

- Added "Common misconceptions" section at the top of `docs/PYTHON_STYLE.md` highlighting frequently violated rules for AI agents.
- Added "DO NOT HIDE BUGS WITH DEFAULTS" section to `docs/PYTHON_STYLE.md` covering `dict.get()` misuse, `value or fallback` patterns, and silent exception swallowing.
- Added "IMPORT REQUIREMENTS" section to `docs/PYTHON_STYLE.md` documenting that all imports must come from stdlib, repo-local modules, or declared pip dependencies.
- Added explicit ban on relative imports (`from . import`) to the IMPORTING section of `docs/PYTHON_STYLE.md`.

### Behavior or Interface Changes

- Clarified shebang rules in `docs/PYTHON_STYLE.md` to explicitly state shebangs belong only on runnable scripts with main guards, not on library modules, test files, or helpers.

## 2026-03-13

### Additions and New Features

- Added `docs/CLAUDE_HOOK_USAGE_GUIDE.md` to `STYLE_FILES` in `propagate_style_guides.py` so it is copied to target repos.
- Added `@docs/CLAUDE_HOOK_USAGE_GUIDE.md` reference to template `CLAUDE.md`.

### Behavior or Interface Changes

- Changed `propagate_style_guides.py` to merge `CLAUDE.md` instead of overwriting, preserving repo-specific `@` reference lines in target repos.

## 2026-02-25

### Fixes and Maintenance

- Fixed `devel/commit_changelog.py` to detect staged (`git add`) changelog changes by falling back to `git diff --cached` when the unstaged diff is empty.

## 2026-02-22

- Updated `docs/REPO_STYLE.md` to require consistent section headings for each changelog day block (`Added`, `Changed`, `Fixed`, `Failures`, `Decisions`) and to keep empty sections with `- None.`.
- Updated `docs/REPO_STYLE.md` section names for changelog day blocks to `Additions`, `Updates`, `Removals`, `Failures`, and `Validations`.
- Updated `docs/REPO_STYLE.md` changelog day template to also require `Fixes` and `Decisions` sections.
- Updated `docs/REPO_STYLE.md` changelog policy language: empty categories are optional, every entry must be categorized, entries are never removed (only rephrased), and day category names are now the six longer labels.

## 2026-02-20

- Added `tests/test_init_files.py` to enforce surface-level `__init__.py` style rules from `docs/PYTHON_STYLE.md`, including checks for non-docstring implementation, imports, exports/maps, global assignments, and `__version__` assignments.
- Scoped `tests/test_init_files.py` to analyze only substantial `__init__.py` files and write violations to `report_init_files.txt` (renamed from `report_init.txt` in 2026-06-11 cleanup) with stale report cleanup at test startup.
- Updated `propagate_style_guides.py` and `.gitignore` to include `test_init_files.py`.
- Simplified gitignore management to require `report_*.txt` and clean up legacy per-report entries in `propagate_style_guides.py`.
- Updated `tests/test_init_files.py` so the no-`__init__.py` case reports pass instead of skip.
- Updated `propagate_style_guides.py` to skip propagating `source_me.sh` into repositories that are already present on `PATH` (for example `junk-drawer`).
- Optimized `tests/test_pyflakes_code_lint.py` to run `pyflakes` once per pytest session and reuse indexed results for per-file tests, preserving one-dot-per-file output while reducing runtime overhead.
- Updated `docs/REPO_STYLE.md` to clarify that changelog entries should capture notable failures and key implementation choices, not only successful changes.

## 2026-02-19

- Added `tests/test_import_dot.py` to fail on relative from-import statements such as `from . import x` and `from .module import x`.
- Updated `propagate_style_guides.py` so `test_import_dot.py` is included in propagated test scripts.
- Updated `tests/test_import_star.py` and `tests/test_import_dot.py` to write per-test report files (`report_import_star.txt` and `report_import_dot.txt`), remove stale reports at test start, and include report paths in assertion failures.
- Renamed `tests/test_import_requirements.py` output to `report_import_requirements.txt` (from `report_imports.txt`) while preserving existing report generation and stale-file cleanup behavior.
- Added import report files to `.gitignore` and `propagate_style_guides.py` required ignore entries: `report_import_star.txt`, `report_import_dot.txt`, and `report_import_requirements.txt`.
- Restored per-file parametrized execution in `tests/test_import_star.py` and `tests/test_import_dot.py` so pytest shows one dot/failure per scanned file while still writing per-test report files.

## 2026-02-16

- Fixed false positives in `tests/test_shebangs.py` where Rust inner attributes (`#![...]`) were misidentified as shebangs, causing `.rs` files to be flagged under `shebang_not_executable`.

## 2026-02-14

- Trimmed `propagate_style_guides.py` to stop editing existing `AGENTS.md` files in target repositories while keeping a no-overwrite bootstrap copy when `AGENTS.md` is missing.
- Added a no-overwrite style file category in `propagate_style_guides.py` so `AGENTS.md` and `docs/AUTHORS.md` are copied only when absent and never updated in-place.
- Updated `propagate_style_guides.py` style destination routing so `CLAUDE.md` is propagated with overwrite to repo root while standard style guides continue to copy into `docs/`.
- Refactored `propagate_style_guides.py` file lists to explicit `(source_name, target_path)` mappings for overwrite and no-overwrite categories, removing special-case destination branching.
- Simplified `propagate_style_guides.py` file lists again to target-relative paths only, deriving source filenames from basename while preserving overwrite/no-overwrite behavior.
- Updated `propagate_style_guides.py` default source lookup/help text to use `<base>/starter_repo_template` instead of `<base>/junk-drawer`.
- Clarified in `README.md` that only `README.md` and `docs/CHANGELOG.md` are repo-specific, while other files are intended to remain generic template infrastructure.
- Standardized `README.md` with a concise infrastructure-focused overview, curated `docs/` links, and a verifiable quick-start test command.
- Updated `AGENTS.md` to direct AI agents to run commands with `bash -lc` (not Zsh) so `source_me.sh` works with expected shell semantics.
