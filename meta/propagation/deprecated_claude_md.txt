# WARNING: Every line below is stripped from every consumer CLAUDE.md
# on each propagation, without per-line origin check. Do NOT add a
# line any consumer might legitimately want to keep.
#
# Each addition to this file must be justified in docs/CHANGELOG.md
# naming the originating cleanup.
#
# Entries are removed by merge_at_imports_safe() in propagate/files.py.

# Fence-marker migration: consumer repos authored under the now-retired
# fenced-merge convention carry these HTML comment lines. Strip on next
# sync so CLAUDE.md returns to a clean @-import list.
<!-- === TEMPLATE-MANAGED START === -->
<!-- === TEMPLATE-MANAGED END === -->

# Historical @-imports retired from the template. Add a line below when
# a docs file is renamed or removed and should disappear from consumers.
# Example:
# @docs/OLD_STYLE.md
